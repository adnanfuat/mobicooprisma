# Migration `20201128094444-u`

This migration has been generated by Tamer UZUN at 11/28/2020, 9:44:44 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201128094444-u
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,3479 @@
+generator client {
+  provider = "prisma-client-js"
+}
+
+datasource db {
+  provider = "mysql"
+  url = "***"
+}
+
+model aktif_kullanicilar {
+  numara       Int       @id @default(autoincrement())
+  user_id      Int?
+  session      String?
+  session_end  DateTime?
+  ip           String?
+  aktif_sayfa  String?
+  son_aktiflik DateTime?
+  mac_adresi   String?
+}
+
+model aktif_kullanici_log {
+  numara    Int       @id @default(autoincrement())
+  kullanici Int?
+  ip        String?
+  tarih     DateTime?
+  sayfa     String?
+}
+
+model alis_fatura_kalemleri {
+  numara                Int     @id @default(autoincrement())
+  fatura_numara         Int     @default(0)
+  stok_urun_numara      Int?
+  miktar                Int?
+  birim                 Int?
+  birim_fiyat           Float?
+  birim_fiyat_doviz     Float?
+  indirim_orani         Int?
+  indirim_tutari        Float?
+  tutar                 Float?
+  kdv_orani             Int?
+  kdv_tutari            Float?
+  net_birim_fiyat       Float?
+  net_birim_fiyat_doviz Float?
+  satir_aciklamasi      String?
+  aciklama              String?
+  para_birimi           String?
+  uygulanan_kur         String?
+}
+
+model araclar {
+  numara         Int       @id @default(autoincrement())
+  plaka          String?
+  ad             String?
+  aktif          String?
+  puan           Int?
+  duzenleyen     String?
+  ekleyen        String?
+  eklenme_tarihi DateTime?
+  resim_adres    String?
+}
+
+model banka_hareketleri {
+  numara                    Int       @id @default(autoincrement())
+  islem_tarihi              DateTime?
+  dekont_tipi               Int?
+  aciklama                  String?
+  cari_sube                 Int?
+  evrak_no                  String?
+  firma                     Int?
+  tutar                     Float?
+  tutar_doviz               Float?
+  ekleyen                   String?
+  ekleyen_numara            Int?
+  eklenme_tarihi            DateTime?
+  duzenleyen                String?
+  duzenleyen_numara         Int?
+  duzenlenme_tarihi         DateTime?
+  hesap_hareketi_numara     Int?
+  kasa_hareketi_numara      Int?
+  banka_hareketi_numara     Int?
+  cek_senet_hareketi_numara Int?
+  banka                     Int?
+  pos                       Int?
+  hesap                     Int?
+  eslestirildi              String?
+  eslestirilme_tarihi       DateTime?
+  eslestirilen_kayit        String?
+  mutabakat_hareketi_numara Int?
+  gider_turu                Int?
+  aktarildi                 String?
+}
+
+model banka_hesap_gecis_sekli {
+  numara Int     @id @default(autoincrement())
+  ad     String?
+  aktif  String?
+  puan   Int?
+}
+
+model banka_mutabakati_hareketler {
+  numara              Int       @id @default(autoincrement())
+  tarih               DateTime
+  aciklama            String
+  tutar               Float
+  bakiye              Float
+  eslestirildi        String
+  banka               Int?
+  hesap               Int
+  eslestirilme_tarihi DateTime
+  eklenme_tarihi      DateTime?
+  ekleyen             String?
+  ekleyen_numara      Int?
+  eslestirilen_kayit  String?
+}
+
+model banka_mutabakatlari {
+  numara            Int       @id @default(autoincrement())
+  islem_tarihi      DateTime?
+  aciklama          String?
+  ekleyen           String?
+  eklenme_tarihi    DateTime?
+  duzenleyen        String?
+  duzenlenme_tarihi DateTime?
+  banka             Int?
+  hesap             Int?
+  ekleyen_numara    Int?
+  duzenleyen_numara Int?
+  kilitlendi        String?
+  kilitlenme_tarihi DateTime?
+}
+
+model bilgilendirmeler {
+  numara               Int       @id @default(autoincrement())
+  aciklama             String?
+  /// This field's default value can currently not be parsed: `0000-00-00 00:00:00`.
+  tarih                DateTime  @default(dbgenerated())
+  ekleyen              String
+  duzenleyen           String
+  durumu               Int       @default(0)
+  bilgilendirme_icerik String?
+  tur                  Int?
+  tur_aciklama         String?
+  kisi_numara          Int?
+  kisi_aciklama        String?
+  firma                Int?
+  firma_aciklama       String?
+  tur_numara           Int?
+  tur_numara_aciklama  String?
+  telefon              String?
+  etiket               String?
+  bilgilendirme_turu   Int?
+  gonderim_durumu      String?
+  gonderilecek_tarih   DateTime?
+  gonderen             String?
+  gonderim_tarihi      DateTime?
+}
+
+model bilgilendirme_antetleri {
+  numara      Int     @id @default(autoincrement())
+  ad          String
+  aciklama    String?
+  resim_adres String
+  aktif       String  @default("")
+  genislik    Int     @default(0)
+  yukseklik   Int     @default(0)
+  link        String?
+  altbilgi    String?
+  blank       String?
+  puan        Int?
+  ekleyen     String?
+  duzenleyen  String?
+  sol_bosluk  Int?
+  sag_bosluk  Int?
+  ust_bosluk  Int?
+  alt_bosluk  Int?
+}
+
+model bilgilendirme_kategori {
+  numara          Int     @id @default(autoincrement())
+  ad              String
+  aktif           String  @default("")
+  ekleyen         String?
+  puan            Int?
+  duzenleyen      String?
+  isturu          Int?
+  ozel_genel      String?
+  uye_numara      Int?
+  hizmet_turu     Int
+  hizmet_baglanti String
+}
+
+model bilgilendirme_loglari {
+  numara               Int       @id @default(autoincrement())
+  bilgilendirme_turu   String?
+  bilgilendirme_tarihi DateTime?
+  firma                Int?
+  eposta               String?
+  gonderen             String?
+  dosya_adres          String?
+}
+
+model bilgilendirme_mesajlari {
+  numara      Int     @id @default(autoincrement())
+  icerik      String
+  aktif       String  @default("")
+  kategori_no Int     @default(0)
+  ekleyen     String?
+  puan        Int?
+  duzenleyen  String?
+  ad          String?
+}
+
+model bilgilendirme_metinleri {
+  numara     Int     @id @default(autoincrement())
+  icerik     String?
+  aktif      String  @default("")
+  alt_icerik String?
+  ekleyen    String?
+  puan       Int?
+  duzenleyen String?
+  ad         String?
+  ozel_genel String?
+  uye_numara Int?
+}
+
+model bolge {
+  numara     Int     @id @default(autoincrement())
+  ad         String
+  aciklama   String
+  aktif      String  @default("")
+  puan       String
+  ekleyen    String?
+  duzenleyen String?
+}
+
+model borc_makbuzu_kalemleri {
+  numara                Int     @id @default(autoincrement())
+  makbuz_numara         Int     @default(0)
+  urun_hizmet_numara    Int?
+  urun_hizmet_turu      String?
+  miktar                Int?
+  birim                 Int?
+  birim_fiyat           Float?
+  birim_fiyat_doviz     Float?
+  indirim_orani         Int?
+  indirim_tutari        Float?
+  tutar                 Float?
+  kdv_orani             Int?
+  kdv_tutari            Float?
+  net_birim_fiyat       Float?
+  net_birim_fiyat_doviz Float?
+  satir_aciklamasi      String?
+  aciklama              String?
+  para_birimi           String?
+  uygulanan_kur         String?
+}
+
+model calisabilme_ihtimali {
+  numara     Int     @id @default(autoincrement())
+  ad         String
+  aciklama   String
+  aktif      String  @default("")
+  puan       Int     @default(0)
+  ekleyen    String?
+  duzenleyen String?
+  ozel_genel String?
+  uye_numara Int?
+}
+
+model cari_hareketleri {
+  numara                       Int       @id @default(autoincrement())
+  tarih                        DateTime?
+  saat                         DateTime?
+  aciklama                     String?
+  firma_numara                 Int?
+  gorusme_numara               Int?
+  ekleyen                      String?
+  eklenme_tarihi               DateTime?
+  duzenleyen                   String?
+  duzenlenme_tarihi            DateTime?
+  musteri_temsilcisi           Int?
+  tutar                        Float?
+  sube                         Int?
+  dekont_tipi                  Int?
+  borc_tipi                    String?
+  ekleyen_numara               Int?
+  duzenleyen_numara            Int?
+  evrak_no                     String?
+  borc_gider_tipi              Int?
+  kasa                         Int?
+  odeme_turu                   Int?
+  kontrol                      String?   @default("0")
+  kontrol_eden                 String?
+  kontrol_eden_numara          Int?
+  otomatik                     String?   @default("0")
+  t_uretimmi                   String?
+  t_is_turu                    Int?
+  t_onay_durumu                String?
+  t_reddedilme_notu            String?
+  t_red_tarihi                 DateTime?
+  t_kilitleme                  String?
+  t_cek_vade_tarihi            DateTime?
+  t_onay_tarihi                DateTime?
+  tahsilat_yeri                Int?
+  gorusme_yeri                 Int?
+  sifir_veya_yenileme_durumu   Int?
+  fatura_numarasi              String?
+  fatura_kesildimi_hukumsuz    String?
+  tip                          String?
+  fatura_tutarlar              String?
+  fatura_logs                  String?
+  banka                        Int?
+  pos_yansimalar               String?
+  pos_yansima_durumu           Int?
+  pos_yansima_baslangic_tarihi DateTime?
+  pos_yansima_bitis_tarihi     DateTime?
+  kasa_banka_aktarim           String?
+  cari_yok                     String?
+  tutar_doviz                  Float?
+  doviz_cinsi                  Int?
+  fatura_tipi                  String?
+  kapali_fatura_makbuz_no      String?
+  uygulanan_kur                Float?
+  kdv_dahil                    String?
+  depo                         Int?
+  gider_turu                   Int?
+  kdv_orani                    Int?
+  kdv_haric_tutar              Float?
+  efatura_UUID                 String?
+  odeme_durumu                 String?
+  kasa_hareketi_numara         Int?
+  banka_hareketi_numara        Int?
+  pos                          Int?
+  gelir_turu                   Int?
+  cek_senet_hareketi_numara    Int?
+  banka_hesap                  Int?
+  aktarildi                    String?
+  urun_hizmet_numara           Int?
+  sozlesme_gerekli             String
+  sozlesme_numarasi            String
+
+  @@index([dekont_tipi], name: "dekont_tipi")
+  @@index([firma_numara], name: "firma_numara")
+}
+
+model dahil_oldugu_gruplar {
+  numara          Int     @id @default(autoincrement())
+  yonetici_numara Int     @default(0)
+  ekleyen         String?
+  duzenleyen      String?
+  grup_numara     Int     @default(0)
+
+  @@index([yonetici_numara], name: "yonetici_numara")
+}
+
+model dekont_tipleri {
+  numara            Int       @id @default(autoincrement())
+  ad                String
+  puan              Int?
+  aktif             String?
+  ekleyen           String?
+  duzenleyen        String?
+  eklenme_tarihi    DateTime?
+  duzenlenme_tarihi DateTime?
+  kullanim          String?
+}
+
+model depo {
+  numara     Int     @id @default(autoincrement())
+  ad         String
+  kisa_ad    String
+  aktif      String  @default("")
+  puan       Int     @default(0)
+  ekleyen    String?
+  duzenleyen String?
+  ozel_genel String?
+  uye_numara Int?
+}
+
+model devamlilik_durumu {
+  numara     Int     @id @default(autoincrement())
+  ad         String
+  aciklama   String
+  aktif      String  @default("")
+  puan       Int     @default(0)
+  ekleyen    String?
+  duzenleyen String?
+  ozel_genel String?
+  uye_numara Int?
+}
+
+model diller {
+  numara         Int     @id @default(autoincrement())
+  ad             String?
+  kod            String?
+  yazim_yonu     String?
+  puan           Int?
+  varsayilan     String? @default("0")
+  aktif          String? @default("0")
+  ad2            String?
+  site_aktif     String?
+  htaccess       String?
+  resim_adres    String?
+  sol_bosluk     Int?    @default(0)
+  sag_bosluk     Int?    @default(0)
+  ust_bosluk     Int?    @default(0)
+  alt_bosluk     Int?    @default(0)
+  siralama_puani Int?
+}
+
+model domainler {
+  numara                     Int     @id @default(autoincrement())
+  ad                         String
+  aktif                      String  @default("")
+  firma                      Int     @default(0)
+  durumu                     Int     @default(0)
+  servis                     Int     @default(0)
+  ekleyen                    String
+  duzenleyen                 String
+  dbaglanti                  Int     @default(0)
+  saktif                     String?
+  aciklama                   String?
+  bilgilendirme_yapilmayacak String?
+
+  @@index([firma], name: "firma")
+}
+
+model domain_baglanti {
+  numara     Int     @id @default(autoincrement())
+  ad         String
+  aktif      String  @default("")
+  puan       Int?
+  kisa_ad    String?
+  aciklama   String?
+  ekleyen    String?
+  duzenleyen String?
+  ozel_genel String?
+  uye_numara Int?
+}
+
+model domain_bilgilendirme {
+  numara     Int      @id @default(autoincrement())
+  domain     Int      @default(0)
+  aciklama   String?
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  tarih      DateTime @default(dbgenerated())
+  gorusulen  String
+  ekleyen    String
+  duzenleyen String
+  durumu     Int      @default(0)
+  aktif      String   @default("")
+  sozlesme   Int?
+  saktif     String?
+  icerik     String?
+}
+
+model domain_bilgilendirme_turu {
+  numara Int    @id @default(autoincrement())
+  ad     String
+  aktif  String @default("")
+}
+
+model domain_durumu {
+  numara Int    @id @default(autoincrement())
+  ad     String
+  aktif  String @default("")
+}
+
+model domain_servis {
+  numara Int    @id @default(autoincrement())
+  ad     String
+  aktif  String @default("")
+}
+
+model domain_sozlesme {
+  numara           Int      @id @default(autoincrement())
+  domain           Int      @default(0)
+  aciklama         String?
+  sozlesme_durumu  String   @default("")
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  baslangic_tarihi DateTime @default(dbgenerated())
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  bitis_tarihi     DateTime @default(dbgenerated())
+  odeme_durumu     String   @default("0")
+  ekleyen          String
+  duzenleyen       String
+  durumu           Int      @default(0)
+  aktif            String   @default("")
+  yil              Int?
+  saktif           String?
+
+  @@index([domain], name: "domain")
+}
+
+model domain_sozlesme_durumu {
+  numara Int    @id @default(autoincrement())
+  ad     String
+  aktif  String @default("")
+}
+
+model efaturalar {
+  numara                                     Int       @id @default(autoincrement())
+  duzenleyen                                 String?
+  ekleyen                                    String?
+  turu                                       String?
+  iptal_edildi                               String?
+  firma                                      Int?
+  eposta                                     String?
+  eposta_gonderim                            String?
+  eklenme_tarihi                             DateTime?
+  Response_Date                              DateTime?
+  Response_Envelope_UUID                     String?
+  Response_Data                              String?
+  Response_GIB_Invoice_ID                    String?
+  Response_UUID                              String?
+  cevap_veren                                String?
+  ID_year                                    Int?
+  ID                                         Int?
+  gonderim_zamani                            DateTime?
+  aciklama                                   String?
+  dekont_no                                  Int?
+  Status_Code                                String?
+  Status_Description                         String?
+  GIB_Status_Code                            String?
+  GIB_Status_Description                     String?
+  InvoiceInfo_UUID                           String?
+  Envelope_UUID                              String?
+  InvoiceInfo_IssueDate                      DateTime?
+  InvoiceInfo_IssueTime                      DateTime?
+  InvoiceInfo_CopyIndicator                  String?
+  InvoiceInfo_Currency                       String?
+  InvoiceInfo_InvoiceID                      String?
+  GIB_Invoice_ID                             String?
+  InvoiceInfo_Direction                      String?
+  InvoiceInfo_Scenario                       String?
+  InvoiceInfo_InvoiceTypeCode                String?
+  InvoiceInfo_SendingType                    String?
+  InvoiceInfo_LineCount                      String?
+  ReceipentInfo_TCKN_VKN                     String?
+  ReceipentInfo_FirstName                    String?
+  ReceipentInfo_PartyTaxScheme               String?
+  ReceipentInfo_FamilyName                   String?
+  ReceipentInfo_PartyName                    String?
+  ReceipentInfo_Address                      String?
+  ReceipentInfo_Country                      String?
+  ReceipentInfo_CityName                     String?
+  ReceipentInfo_CitySubdivisionName          String?
+  ReceipentInfo_WebURL                       String?
+  ReceipentInfo_Telephone                    String?
+  SenderInfo_TCKN_VKN                        String?
+  SenderInfo_FirstName                       String?
+  SenderInfo_FamilyName                      String?
+  SenderInfo_PartyName                       String?
+  SenderInfo_PartyTaxScheme                  String?
+  SenderInfo_Address                         String?
+  SenderInfo_Country                         String?
+  SenderInfo_CityName                        String?
+  SenderInfo_CitySubdivisionName             String?
+  SenderInfo_WebURL                          String?
+  SenderInfo_Telephone                       String?
+  TaxAmount_Amount                           String?
+  TaxAmount_Currency                         String?
+  TaxSubTotals_TaxableAmount_Currency        String?
+  TaxSubTotals_TaxableAmount_Amount          String?
+  TaxSubTotals_TaxAmount_Currency            String?
+  TaxSubTotals_TaxAmount_Amount              String?
+  TaxSubTotals2_TaxableAmount_Currency       String?
+  TaxSubTotals2_TaxableAmount_Amount         String?
+  TaxSubTotals2_TaxAmount_Currency           String?
+  TaxSubTotals2_TaxAmount_Amount             String?
+  TaxSubTotals3_TaxableAmount_Currency       String?
+  TaxSubTotals3_TaxableAmount_Amount         String?
+  TaxSubTotals3_TaxAmount_Currency           String?
+  TaxSubTotals3_TaxAmount_Amount             String?
+  TaxScheme_Name                             String?
+  TaxScheme_TaxTypeCode                      String?
+  InvoiceTotals_LineExtensionAmount_Currency String?
+  InvoiceTotals_LineExtensionAmount_Amount   String?
+  InvoiceTotals_TaxExclusiveAmount_Currency  String?
+  InvoiceTotals_TaxExclusiveAmount_Amount    String?
+  InvoiceTotals_TaxInclusiveAmount_Currency  String?
+  InvoiceTotals_TaxInclusiveAmount_Amount    String?
+  InvoiceTotals_PayableAmount_Currency       String?
+  InvoiceTotals_PayableAmount_Amount         String?
+  Taxes_Withholding                          String?
+}
+
+model efatura_ayarlari {
+  numara                        Int     @id @default(autoincrement())
+  unvan                         String
+  ad                            String
+  soyad                         String
+  tc                            String
+  vkn                           String?
+  web                           String?
+  eposta                        String?
+  il                            String?
+  adres                         String?
+  ilce                          String?
+  ulke                          String?
+  vergi_dairesi                 String?
+  telefon                       String?
+  ticaret_sicil_no              String?
+  mersis_no                     String?
+  webservis_kullanici           String?
+  webservis_firma_kodu          String?
+  webservis_sifre               String?
+  gonderici_birim_etiketi       String?
+  posta_kutusu                  String?
+  gonderim_turu                 String?
+  fatura_not                    String?
+  fatura_gonderim_eposta_adresi String?
+  fatura_onek                   String?
+  arsiv_onek                    String?
+  ekleyen                       String?
+  duzenleyen                    String?
+  fatura_imza_resim             String
+}
+
+model efatura_kalemleri {
+  numara                                  Int     @id @default(autoincrement())
+  efatura_numara                          Int     @default(0)
+  Line_ID                                 Int?
+  UnitCode                                String?
+  Quantity                                Int?
+  LineExtensionAmount_Amount              Float?
+  LineExtensionAmount_Currency            String?
+  AllowanceChange_ChargeIndicator         String?
+  AllowanceChange_MultiplierFactorNumeric String
+  ChargeAmount_Currency                   String?
+  ChargeAmount_Amount                     String?
+  Item_Name                               String?
+  Item_Description                        String?
+  Item_Keyword                            String?
+  Price_Currency                          String?
+  Price_Amount                            String?
+  Tax_Amount                              String?
+  Tax_Rate                                String?
+  Other_Tax                               String?
+}
+
+model fatura_sablonlari {
+  numara                        Int     @id @default(autoincrement())
+  ad                            String
+  aktif                         String  @default("")
+  varsayilan                    String  @default("0")
+  sayfa_ozellik                 String
+  kopya_yonu                    String?
+  sayfadaki_kopya_sayisi        Int
+  listelenecek_urun_hizmet_sayi Int     @default(0)
+  fatura_genel_alanlar          String?
+  musteri_unvani                String?
+  musteri_adresi                String?
+  musteri_vd                    String?
+  musteri_vn                    String?
+  fatura_numarasi               String?
+  fatura_tarihi                 String?
+  kdv_haric_toplam              String?
+  kdv_toplam                    String?
+  kdv_dahil_toplam              String?
+  hizmet_urun_table_tile        String?
+  fatura_kalemi_alanlar         String?
+  fk_hizmet_tanimi              String?
+  fk_miktar                     String?
+  fk_urun_hizmet_kodu           String?
+  fk_birim_fiyat                String?
+  fk_kdv_orani                  String?
+  fk_kdv_tutari                 String?
+  fk_tutar                      String?
+  ekleyen                       String?
+  duzenleyen                    String?
+  resim_adres                   String?
+}
+
+model filtre_veriler {
+  numara            Int      @id @default(autoincrement())
+  param1            String?
+  param2            String?
+  param3            String?
+  param4            String?
+  param5            String?
+  param6            String?
+  param7            String?
+  param8            String?
+  param9            String?
+  param10           String?
+  param11           String?
+  param12           String?
+  param13           String?
+  param14           String?
+  param15           String?
+  param16           String?
+  eklenme_tarihi    DateTime
+  islem_tarihi      DateTime
+  gecerlilik_tarihi DateTime
+  data              String
+}
+
+model firma {
+  numara                               Int       @id @default(autoincrement())
+  ad                                   String
+  resmi_unvan                          String?
+  aktif                                String    @default("")
+  puan                                 String
+  ekleyen                              String?
+  duzenleyen                           String?
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  eklenme_tarihi                       DateTime  @default(dbgenerated())
+  musteri_bilgi                        Int?
+  musteri_bilgi_guncelleme             String?
+  mib_numara                           Int       @default(0)
+  bolge                                Int?
+  web                                  String?
+  duzenlenme_tarihi                    DateTime?
+  il                                   Int?
+  ilce                                 Int?
+  cv_bilgi                             String?
+  cv_bilgi_guncelleme                  String?
+  devamlilik_guncelleme                String?
+  calisabilme_guncelleme               String?
+  link1                                String?
+  link2                                String?
+  link3                                String?
+  link4                                String?
+  link5                                String?
+  link6                                String?
+  link7                                String?
+  link8                                String?
+  musteri_temsilcisi                   Int?
+  musteri_bilgi_guncelleme_tarihi      DateTime?
+  devamlilik_guncelleme_tarihi         DateTime?
+  calisabilme_guncelleme_tarihi        DateTime?
+  musteri_temsilcisi_guncelleme        String?
+  musteri_temsilcisi_guncelleme_tarihi DateTime?
+  devamlilik                           Int?
+  calisabilme                          Int?
+  diger                                String?
+  otomatik_kayit                       String?
+  ozel                                 String?
+  ozel_genel                           String
+}
+
+model firmalar_ozellikler {
+  numara         Int    @id @default(autoincrement())
+  firma_numara   Int    @default(0)
+  ozellik_numara Int    @default(0)
+  aktif          String @default("")
+}
+
+model firmalar_sektorler {
+  numara         Int       @id @default(autoincrement())
+  sektor         Int?
+  firma_numara   Int?
+  ekleyen        String?
+  eklenme_tarihi DateTime?
+}
+
+model firma_adres {
+  numara            Int       @id @default(autoincrement())
+  firma_no          Int       @default(0)
+  aktif             String    @default("")
+  adres             String
+  ekleyen           String?
+  duzenleyen        String?
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  eklenme_tarihi    DateTime  @default(dbgenerated())
+  il                Int?
+  ilce              Int?
+  duzenlenme_tarihi DateTime?
+}
+
+model firma_detay {
+  numara         Int      @id @default(autoincrement())
+  firma_no       Int      @default(0)
+  aktif          String   @default("")
+  bolge          Int      @default(0)
+  ekleyen        String?
+  duzenleyen     String?
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  eklenme_tarihi DateTime @default(dbgenerated())
+}
+
+model firma_ek {
+  numara                      Int       @id @default(autoincrement())
+  mt                          Int       @default(0)
+  resim_adres                 String
+  beklemedeki_kayit           String
+  aktif                       String    @default("")
+  ad                          String?
+  ekleyen                     String?
+  duzenleyen                  String?
+  firma                       Int?
+  eklenme_tarihi              DateTime?
+  duzenlenme_tarihi           DateTime?
+  aciklama                    String?
+  gorusme_numara              Int?      @default(0)
+  ekleyen_numara              Int?
+  duzenleyen_numara           Int?
+  ise_bagli                   String?
+  google_drive_kayit_aciklama String?
+  google_drive                String?
+  drive_sevk_edildi           String?
+  konusma_yaziya_cevrildi     String?
+  yaziya_cevrilme_tarihi      DateTime?
+  yaziya_cevrilme_saati       DateTime?
+  sanal_santral               String?
+  ss_linkedid                 String?
+  ss_uniqueid                 String?
+  sanal_santral_onay          String?
+  sanal_santral_detay         String?
+  onaylayan                   Int?
+  kayit_bilgilendirme         String?
+  gorusme_tarihi              DateTime?
+  onemli                      String?
+  cagri_yonu                  String?
+  egitim                      String?
+  ek_silindi                  String?
+  hedef                       String?
+  kaynak                      String?
+  sure                        Int?
+  ses_var                     String?
+  durum                       String?
+  realsrc                     String?
+  silinecek                   String
+}
+
+model firma_faks {
+  numara            Int       @id @default(autoincrement())
+  etiket            String?
+  telefonkod        String?
+  telefon           String?
+  firma             Int?
+  aktif             String?
+  puan              Int?
+  varsayilan        String?
+  pbxmi             String?
+  hatsayi           Int?
+  ekleyen           String?
+  duzenleyen        String?
+  eklenme_tarihi    DateTime?
+  duzenlenme_tarihi DateTime?
+}
+
+model firma_fatura_bilgi {
+  numara            Int       @id @default(autoincrement())
+  ad_soyad          String
+  tc_kimlik         String
+  adres             String
+  resmi_unvan       String
+  vergi_dairesi     String
+  vergi_numarasi    String
+  fatura_tipi       String
+  ekleyen           String
+  ekleyen_numara    Int
+  eklenme_tarihi    DateTime
+  eposta            String
+  firma             Int
+  eposta_verildi    String?
+  il                Int?
+  ilce              Int?
+  ad                String?
+  soyad             String?
+  duzenlenme_tarihi DateTime?
+  duzenleyen        String?
+  duzenleyen_numara Int?
+  varsayilan        String?
+}
+
+model firma_fatura_bilgileri {
+  numara              Int      @id @default(autoincrement())
+  ad_soyad            String
+  tc_kimlik           String
+  adres               String
+  resmi_unvan         String
+  vergi_dairesi       String
+  vergi_numarasi      String
+  fatura_tipi         String
+  ekleyen             String
+  ekleyen_numara      Int
+  eklenme_tarihi      DateTime
+  eposta              String
+  firma               Int
+  eposta_verildi      String?
+  il                  Int?
+  ilce                Int?
+  fatura_bilgi_numara Int?
+  ad                  String?
+  soyad               String?
+  varsayilan          String?
+}
+
+model firma_gsm {
+  numara            Int       @id @default(autoincrement())
+  etiket            String?
+  telefonkod        String?
+  telefon           String?
+  firma             Int?
+  aktif             String?
+  puan              Int?
+  varsayilan        String?
+  bilgi_mesaj       String?
+  ekleyen           String?
+  duzenleyen        String?
+  eklenme_tarihi    DateTime?
+  duzenlenme_tarihi DateTime?
+}
+
+model firma_mail {
+  numara            Int       @id @default(autoincrement())
+  etiket            String?
+  mail              String?
+  firma             Int?
+  aktif             String?
+  puan              Int?
+  varsayilan        String?
+  ekleyen           String?
+  duzenleyen        String?
+  eklenme_tarihi    DateTime?
+  duzenlenme_tarihi DateTime?
+  bilgi_mesaj       String?
+}
+
+model firma_mt_gorusme_gizle {
+  numara         Int      @id @default(autoincrement())
+  mt             Int      @default(0)
+  firma          Int      @default(0)
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  eklenme_tarihi DateTime @default(dbgenerated())
+
+  @@index([firma], name: "firma")
+}
+
+model firma_ozellik {
+  numara      Int     @id @default(autoincrement())
+  ad          String
+  aciklama    String
+  aktif       String  @default("")
+  puan        Int     @default(0)
+  ekleyen     String?
+  duzenleyen  String?
+  resim_adres String?
+  ozel_genel  String?
+  uye_numara  Int?
+}
+
+model firma_resim {
+  numara            Int       @id @default(autoincrement())
+  resim_adres       String
+  aktif             String    @default("")
+  ad                String?
+  ekleyen           String?
+  duzenleyen        String?
+  firma             Int?
+  eklenme_tarihi    DateTime?
+  duzenlenme_tarihi DateTime?
+  aciklama          String?
+}
+
+model firma_telefon {
+  numara            Int       @id @default(autoincrement())
+  etiket            String?
+  telefonkod        String?
+  telefon           String?
+  firma             Int?
+  aktif             String?
+  puan              Int?
+  varsayilan        String?
+  pbxmi             String?
+  hatsayi           Int?
+  ekleyen           String?
+  duzenleyen        String?
+  eklenme_tarihi    DateTime?
+  duzenlenme_tarihi DateTime?
+}
+
+// The underlying table does not contain a valid unique identifier and can therefore currently not be handled.
+// model gecici_tablo {
+  // numara       Int
+  // mail         String
+  // firma        Int?
+  // domain       Int?
+  // domain_adi   String?
+  // firma_adi    String?
+  // telefonkod   String?
+  // telefon      String?
+  // bitis_tarihi DateTime?
+// }
+
+model gelistirme_talepleri {
+  numara            Int       @id @default(autoincrement())
+  ad                String
+  aktif             String    @default("")
+  icerik            String?
+  puan              Int?
+  kullanici         Int?
+  ekleyen           String?
+  duzenleyen        String?
+  eklenme_tarihi    DateTime?
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  guncelleme_tarihi DateTime? @default(dbgenerated())
+  talep_durumu      Int?
+}
+
+model gider_fatura_kalemleri {
+  numara                Int     @id @default(autoincrement())
+  fatura_numara         Int     @default(0)
+  gider_kodu            Int?
+  miktar                Int?
+  birim                 Int?
+  birim_fiyat           Float?
+  birim_fiyat_doviz     Float?
+  indirim_orani         Int?
+  indirim_tutari        Float?
+  tutar                 Float?
+  kdv_orani             Int?
+  kdv_tutari            Float?
+  net_birim_fiyat       Float?
+  net_birim_fiyat_doviz Float?
+  satir_aciklamasi      String?
+  aciklama              String?
+  para_birimi           String?
+  uygulanan_kur         String?
+}
+
+model gider_turu {
+  numara     Int     @id @default(autoincrement())
+  ad         String
+  kisa_ad    String
+  aktif      String  @default("")
+  puan       Int     @default(0)
+  ekleyen    String?
+  duzenleyen String?
+  ozel_genel String?
+  uye_numara Int?
+}
+
+model gidis_durumu {
+  numara     Int     @id @default(autoincrement())
+  ad         String
+  kisa_ad    String
+  aktif      String  @default("")
+  puan       Int     @default(0)
+  ekleyen    String?
+  duzenleyen String?
+  ozel_genel String?
+  uye_numara Int?
+}
+
+model gorevlendirmeler {
+  numara                     Int      @id @default(autoincrement())
+  gorev_numara               Int
+  surec_numara               Int
+  ana_gorev_numara           Int
+  ana_gorev                  String
+  tetikleyici_gorev_numara   Int
+  baslik                     String
+  tarih                      DateTime
+  saat                       DateTime
+  termin_dk                  Int
+  termin_saat                Int
+  termin_gun                 Int
+  eklenme_tarihi             DateTime
+  eklenme_saati              DateTime
+  aktif                      String
+  bitirilmesi_gereken_tarih  DateTime
+  bitirilmesi_gereken_saat   DateTime
+  bitirme_tarihi             DateTime
+  bitirme_saati              DateTime
+  bitirme_notu               String
+  zamanlama                  String
+  aciklama                   String?
+  hukmunu_kaybetti           String
+  cebren_dusuruldu           String
+  otomatik_bitirilme_tarihi  DateTime
+  otomatik_bitirilme_saati   DateTime
+  reddeden                   String
+  bitirme_ek1                String
+  bitirme_ek2                String
+  bitirme_ek3                String
+  bitirme_ek4                String
+  bitirme_ek5                String
+  bitirme_ek6                String
+  bitirme_ek7                String
+  bitirme_ek8                String
+  bitirme_ek9                String
+  bitirme_ek10               String
+  bitirme_ek11               String
+  bitirme_ek12               String
+  bitirme_ek13               String
+  bitirme_ek14               String
+  bitirme_ek15               String
+  bitirme_ek16               String
+  bitirme_ek17               String
+  bitirme_ek18               String
+  bitirme_ek19               String
+  bitirme_ek20               String
+  gorev_ek1                  String
+  gorev_ek2                  String
+  gorev_ek3                  String
+  gorev_ek4                  String
+  gorev_ek5                  String
+  gorev_ek6                  String
+  gorev_ek7                  String
+  gorev_ek8                  String
+  gorev_ek9                  String
+  gorev_ek10                 String
+  gorev_ek11                 String
+  gorev_ek12                 String
+  gorev_ek13                 String
+  gorev_ek14                 String
+  gorev_ek15                 String
+  gorev_ek16                 String
+  gorev_ek17                 String
+  gorev_ek18                 String
+  gorev_ek19                 String
+  gorev_ek20                 String
+  bitirme_link1              String
+  bitirme_link               String
+  bitirme_link2              String
+  bitirme_link3              String
+  bitirme_link4              String
+  bitirme_link5              String
+  bitirme_link6              String
+  reddeden_numara            Int
+  reddedilme_notu_kontrol    String
+  reddedilme_notu            String
+  reddedilme_tarihi          DateTime
+  reddedilme_saati           DateTime
+  muhlet_bitis_tarihi        DateTime
+  muhlet_bitis_saati         DateTime
+  log                        String?
+  firma                      Int
+  durumu                     String
+  ilk_bildirim_maili         String
+  baslangic_bildirim_maili   String
+  zamaninda                  String
+  kontrol_notu               String
+  kontrol                    String
+  kontrol_eden               String
+  kontrol_eden_numara        Int
+  kontrol_tarihi             DateTime
+  kontrol_saati              DateTime
+  denetleme                  String
+  denetleyen                 String
+  denetleyen_numara          Int
+  personel_numara            Int
+  denetleme_tarihi           DateTime
+  denetleme_saati            DateTime
+  ekleyen                    String
+  duzenleyen                 String
+  cebren_dusuren             String
+  ekleyen_numara             Int
+  anlik_kategori_aciklama    String?
+  duzenleyen_numara          Int
+  cebren_dusuren_numara      Int
+  opsiyonel                  String
+  hukmen_dussun_mu           String?
+  prim_hesaplanacak          String?
+  prim_hesaplanacak_ilk      String?
+  son_prim                   Float?
+  prim                       Float?
+  gorev_sonrasi_prim         Float?
+  sistem_prim                Float?
+  prim_degerini_yoksay       String?
+  son_prim_ust_carpan        Float?
+  son_prim_alt_carpan        Float?
+  kural_numara               Int?
+  gorusme_numara             Int?
+  gorev_baglantili_numara    String?
+  gorev_baglantili_konum     String?
+  onemli                     String?
+  gorev_parametreleri        String?
+  simulasyon_gorevi          String?
+  zaman_kisitlama            String?
+  zorunlu_prim               String?
+  rezervasyon_anlasma_no     Int
+  gec_bitirilme_yoksay       String
+
+  @@index([gorev_numara], name: "gorev_numara")
+  @@index([personel_numara], name: "personel_numara")
+}
+
+model gorevlendirmeler_detay {
+  numara               Int    @id @default(autoincrement())
+  gorevlendirme_numara Int
+  ek                   String
+}
+
+model gorevler {
+  numara                          Int       @id @default(autoincrement())
+  ad                              String
+  kisa_ad                         String
+  aktif                           String    @default("")
+  puan                            Int       @default(0)
+  ekleyen                         String?
+  duzenleyen                      String?
+  onay_gerektiriyor               Int       @default(0)
+  renk                            String?
+  kutuphaneden_aktarim_tarihi     DateTime?
+  kutuphane_numara                Int?
+  gorevli_sure_duzenleme          String?
+  erken_kapatma_izni              String
+  ekleyenin_duzenleyebilecegi_gun Int
+  ozel_genel                      String
+}
+
+model gorevler_bilgilendirme_islem_turu {
+  numara                  Int     @id @default(autoincrement())
+  ad                      String
+  kod                     String
+  aktif                   String  @default("")
+  aciklama                String
+  puan                    Int     @default(0)
+  ekleyen                 String?
+  duzenleyen              String?
+  gorevli_varsayilan      String?
+  kontrol_eden_varsayilan String?
+  denetleyen_varsayilan   String?
+}
+
+model gorevler_bilgilendirme_islem_yetkileri {
+  numara            Int       @id @default(autoincrement())
+  yetkili           String?
+  ekleyen           String?
+  gorev_altkategori Int       @default(0)
+  duzenleyen        String
+  islem_turu        Int       @default(0)
+  eklenme_tarihi    DateTime?
+}
+
+model gorev_altturleri {
+  numara                                  Int       @id @default(autoincrement())
+  ad                                      String
+  prim_sonradan_belirlenecek_isaretlensin String?
+  kisa_ad                                 String
+  aktif                                   String    @default("")
+  puan                                    Int       @default(0)
+  ekleyen                                 String?
+  duzenleyen                              String?
+  on_tanimli_personel                     Int       @default(0)
+  renk                                    String?
+  kategori                                Int?
+  onem_derecesi                           Int?
+  hukmen_dussun_mu                        String?
+  kapanma_bilgilendirme_notu              String?
+  aciklama                                String?
+  ongorulen_yapim_suresi                  Int?
+  muhlet_dk                               Int?
+  ek1                                     String?
+  ek2                                     String?
+  ek3                                     String?
+  ek4                                     String?
+  ek5                                     String?
+  ek6                                     String?
+  ek7                                     String?
+  ek8                                     String?
+  ek9                                     String?
+  ek10                                    String?
+  kontrol_eden                            Int?
+  denetleyen                              Int?
+  koordine_eden                           String?
+  gonderilmeyecek_bildirimler             String?
+  ogak_ile_ileri_atama_yapma              String?
+  koordine_ve_on_tanimli_iptal            String?
+  kontrol_takip_termin_gun                Int?
+  kontrol_takip_termin_saat               Int?
+  kontrol_takip_termin_dk                 Int?
+  denetleme_takip_termin_gun              Int?
+  denetleme_takip_termin_saat             Int?
+  denetleme_takip_termin_dk               Int?
+  tetiklenecekler                         String?
+  kutuphaneden_aktarim_tarihi             DateTime?
+  kutuphane_numara                        Int?
+  gorevli_sure_duzenleme                  String
+  ekleyenin_duzenleyebilecegi_gun         Int
+  ozel_genel                              String
+}
+
+model gorev_simulasyon_parametreleri {
+  numara            Int       @id @default(autoincrement())
+  baslik            String
+  pga_numara        Int
+  deger             Int?
+  g_kategori        Int?
+  g_altkategori     Int?
+  duzenleme_hakki   Int?
+  aktif             String?
+  ekleyen           String?
+  duzenleyen        String?
+  eklenme_tarihi    DateTime?
+  duzenlenme_tarihi DateTime?
+}
+
+model gorev_surecleri {
+  numara                 Int       @id @default(autoincrement())
+  ad                     String
+  gorev_altturu          Int
+  aktif                  String
+  ekleyen                String
+  duzenleyen             String
+  puan                   Int       @default(0)
+  prim                   Float?
+  prim_duzenleyen        String?
+  prim_duzenlenme_tarihi DateTime?
+  prim_hesaplanacak      String?
+  on_tanimli_personel    String
+  muhlet_dk              Int?
+  muhlet_saat            Int?
+  opsiyonel              String?
+  muhlet_gun             Int?
+  ozel_genel             String
+}
+
+model gorusmeler {
+  numara                  Int       @id @default(autoincrement())
+  firma                   Int       @default(0)
+  referans                Int       @default(0)
+  gidis_durumu            Int       @default(0)
+  aciklama                String
+  mtemsilcisi             Int?
+  aktif                   String?
+  neticesiz_is_durumu     Int?
+  eklenme_tarihi          DateTime?
+  islembitti              String    @default("")
+  tarih                   DateTime?
+  onaylandi               String    @default("")
+  gorusme_turu            Int       @default(0)
+  olumluluk_derecesi      String?
+  ekleyen                 String?
+  duzenleyen              String?
+  gorusmeyle_ilgili_yorum String?
+  bitis_tarihi            DateTime?
+  moderator_sonuc_yorumu  Int?      @default(0)
+  hatirlatma_tarihi       DateTime?
+  hatirlatma_onemli       String?
+  baslangic_saati         DateTime?
+  bitis_saati             DateTime?
+  arac_bilgi              String?
+  as_sabitle              String?
+  sebep_goruldu           String?
+  hatirlatma_silindi      String?
+  vekaleten_eklenen       Int?
+  onaya_gonderim_tarihi   DateTime?
+  gorusme_duygu           String?
+
+  @@index([bitis_tarihi], name: "bitis_tarihi")
+  @@index([firma], name: "firma")
+  @@index([mtemsilcisi], name: "mtemsilcisi")
+  @@index([tarih], name: "tarih")
+}
+
+model gorusme_duygu_ikonlari {
+  numara      Int     @id @default(autoincrement())
+  ad          String
+  aciklama    String
+  aktif       String  @default("")
+  puan        Int     @default(0)
+  ekleyen     String?
+  duzenleyen  String?
+  resim_adres String?
+  ozel_genel  String?
+  uye_numara  Int?
+}
+
+model gorusme_gorusulen_kisi {
+  numara         Int     @id @default(autoincrement())
+  kisi_numara    Int     @default(0)
+  gorusme_numara Int     @default(0)
+  ekleyen        String?
+  duzenleyen     String?
+  kisi           String?
+
+  @@index([gorusme_numara], name: "gorusme_numara")
+}
+
+model gorusme_hatirlatmalari_mailing {
+  numara          Int       @id @default(autoincrement())
+  tarih           DateTime?
+  gonderen        String?
+  gonderen_numara Int?
+}
+
+model gorusme_isler {
+  numara                 Int       @id @default(autoincrement())
+  isturu                 Int       @default(0)
+  isturu_detay           Int       @default(0)
+  ad                     String
+  aciklama               String
+  gorusme_numara         Int       @default(0)
+  mtemsilcisi            Int       @default(0)
+  firma_numara           Int       @default(0)
+  is_durumu              Int       @default(0)
+  iskapat                Int       @default(0)
+  moderator_yorumu       String
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  baslangic_tarihi       DateTime  @default(dbgenerated())
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  bitis_tarihi           DateTime  @default(dbgenerated())
+  ucret                  String    @default("")
+  hedef_musteri_bilgisi  Int       @default(0)
+  ekleyen                String?
+  duzenleyen             String?
+  moderator_sonuc_yorumu Int?      @default(0)
+  is_yapim_sure          Float?
+  teknik_kapama_notu     String?
+  teknik_aciklama        String?
+  sebep_goruldu          String?
+  onaya_gonderim_tarihi  DateTime?
+  teknik_duzenleyen      Int?
+  baslangic_onayi        String?
+  baslangic_onayi_veren  String?
+
+  @@index([firma_numara], name: "firma_numara")
+  @@index([gorusme_numara], name: "gorusme_numara")
+}
+
+model guvenli_ipler {
+  numara      Int     @id @default(autoincrement())
+  ip_adresi   String
+  ipv6_adresi String
+  aktif       String  @default("")
+  puan        Int     @default(0)
+  ekleyen     String?
+  duzenleyen  String?
+}
+
+model g_okundu {
+  numara           Int      @id @default(autoincrement())
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  eklenme_tarihi   DateTime @default(dbgenerated())
+  okuyan_kullanici Int      @default(0)
+  okunan_gorusme   Int      @default(0)
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  okunan_tarih     DateTime @default(dbgenerated())
+
+  @@index([okunan_gorusme], name: "okunan_gorusme")
+  @@index([okuyan_kullanici], name: "okuyan_kullanici")
+}
+
+model hizmet_kesinti_sekli {
+  numara Int     @id @default(autoincrement())
+  ad     String?
+  aktif  String?
+  puan   Int?
+}
+
+model hostingler {
+  numara     Int     @id @default(autoincrement())
+  aktif      String  @default("")
+  domain     Int     @default(0)
+  durumu     Int     @default(0)
+  servis     Int     @default(0)
+  ekleyen    String
+  duzenleyen String
+  saktif     String?
+  aciklama   String?
+  dbaglanti  Int     @default(0)
+  firma      Int     @default(0)
+
+  @@index([firma], name: "firma")
+}
+
+model hosting_bilgilendirme {
+  numara     Int      @id @default(autoincrement())
+  hosting    Int      @default(0)
+  aciklama   String?
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  tarih      DateTime @default(dbgenerated())
+  gorusulen  String
+  ekleyen    String
+  duzenleyen String
+  durumu     Int      @default(0)
+  aktif      String   @default("")
+  sozlesme   Int?
+  saktif     String?
+  icerik     String?
+}
+
+model hosting_durumu {
+  numara Int    @id @default(autoincrement())
+  ad     String
+  aktif  String @default("")
+}
+
+model hosting_logs {
+  numara             Int      @id @default(autoincrement())
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  islem_tarihi       DateTime @default(dbgenerated())
+  islem_tipi         String
+  islem_yapan        String
+  kontrol            String?
+  kontrol_eden       String?
+  k_tablo            String
+  k_numara           Int      @default(0)
+  k_ad               String
+  k_aktif            String   @default("")
+  k_domain           Int      @default(0)
+  k_domain_ad        String?
+  k_aciklama         String?
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  k_tarih            DateTime @default(dbgenerated())
+  k_gorusulen        String
+  k_ekleyen          String
+  k_duzenleyen       String
+  k_durumu           Int      @default(0)
+  k_durumu_ad        String?
+  k_sozlesme         Int      @default(0)
+  k_sozlesme_tarih   String?
+  k_yetkili          String
+  k_mt               Int      @default(0)
+  k_mt_ad            String?
+  k_sozlesme_durumu  String   @default("")
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  k_baslangic_tarihi DateTime @default(dbgenerated())
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  k_bitis_tarihi     DateTime @default(dbgenerated())
+  k_odeme_durumu     String   @default("")
+  k_yil              Int      @default(0)
+  k_firma            Int      @default(0)
+  k_firma_ad         String?
+  k_servis           Int      @default(0)
+  k_servis_ad        String?
+  k_dbaglanti        Int      @default(0)
+  k_dbaglanti_ad     String?
+  k_saktif           String?
+  k_icerik           String?
+  k_buyuklugu        Int      @default(0)
+}
+
+model hosting_servis {
+  numara Int    @id @default(autoincrement())
+  ad     String
+  aktif  String @default("")
+}
+
+model hosting_sozlesme {
+  numara           Int      @id @default(autoincrement())
+  hosting          Int      @default(0)
+  aciklama         String?
+  sozlesme_durumu  String   @default("")
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  baslangic_tarihi DateTime @default(dbgenerated())
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  bitis_tarihi     DateTime @default(dbgenerated())
+  odeme_durumu     String   @default("0")
+  ekleyen          String
+  duzenleyen       String
+  durumu           Int      @default(0)
+  aktif            String   @default("")
+  yil              Int?
+  saktif           String?
+  buyuklugu        Int      @default(0)
+
+  @@index([hosting], name: "hosting")
+}
+
+model hosting_sozlesme_durumu {
+  numara Int    @id @default(autoincrement())
+  ad     String
+  aktif  String @default("")
+}
+
+model il {
+  numara  Int     @id @default(autoincrement())
+  ad      String
+  aktif   String  @default("")
+  ekleyen String?
+  puan    Int?
+}
+
+model ilce {
+  numara  Int     @id @default(autoincrement())
+  ad      String
+  aktif   String  @default("")
+  il_no   Int     @default(0)
+  ekleyen String?
+  puan    Int?
+}
+
+model isin_teknik_durumu {
+  numara     Int     @id @default(autoincrement())
+  ad         String
+  aktif      String  @default("")
+  aciklama   String
+  puan       Int     @default(0)
+  ekleyen    String?
+  duzenleyen String?
+}
+
+model isler {
+  numara                            Int     @id @default(autoincrement())
+  ad                                String
+  kisa_ad                           String
+  aktif                             String  @default("")
+  puan                              Int     @default(0)
+  ekleyen                           String?
+  duzenleyen                        String?
+  onay_gerektiriyor                 Int     @default(0)
+  renk                              String?
+  ozel_genel                        String?
+  uye_numara                        Int?
+  kdv_orani                         Int?
+  kod                               String?
+  populer_is                        String?
+  sozlesmesi_rezervasyon_gerektirir String?
+}
+
+model is_altturleri {
+  numara              Int     @id @default(autoincrement())
+  ad                  String
+  kisa_ad             String
+  aktif               String  @default("")
+  puan                Int     @default(0)
+  ekleyen             String?
+  duzenleyen          String?
+  on_tanimli_personel Int     @default(0)
+  renk                String?
+  isturu_numara       Int?
+}
+
+model is_durumu {
+  numara     Int     @id @default(autoincrement())
+  ad         String
+  aktif      String  @default("")
+  aciklama   String
+  puan       Int     @default(0)
+  ekleyen    String?
+  duzenleyen String?
+}
+
+model kasa {
+  numara      Int     @id @default(autoincrement())
+  ad          String
+  kisa_ad     String
+  aktif       String  @default("")
+  puan        Int     @default(0)
+  ekleyen     String?
+  duzenleyen  String?
+  ozel_genel  String?
+  uye_numara  Int?
+  dekont_tipi Int?
+  doviz_cinsi Int?
+}
+
+model kasa_hareketleri {
+  numara                    Int       @id @default(autoincrement())
+  islem_tarihi              DateTime?
+  dekont_tipi               Int?
+  aciklama                  String?
+  sube                      Int?
+  evrak_no                  String?
+  firma                     Int?
+  tutar                     Float?
+  tutar_doviz               Float?
+  ekleyen                   String?
+  ekleyen_numara            Int?
+  eklenme_tarihi            DateTime?
+  duzenleyen                String?
+  duzenleyen_numara         Int?
+  duzenlenme_tarihi         DateTime?
+  hesap_hareketi_numara     Int?
+  kasa_hareketi_numara      Int?
+  banka_hareketi_numara     Int?
+  cek_senet_hareketi_numara Int?
+  kasa                      Int?
+  odeme_turu                Int?
+  aktarildi                 String?
+}
+
+model kesinti_sekilleri {
+  numara Int     @id @default(autoincrement())
+  ad     String?
+  aktif  String?
+  puan   Int?
+}
+
+model kisiler {
+  numara            Int       @id @default(autoincrement())
+  ad                String
+  aktif             String    @default("")
+  firma_no          Int       @default(0)
+  ekleyen           String?
+  duzenleyen        String?
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  eklenme_tarihi    DateTime  @default(dbgenerated())
+  gorev             Int?
+  yorum             Int?
+  duzenlenme_tarihi DateTime?
+  dogum_tarihi      DateTime?
+}
+
+model kisi_detay {
+  numara         Int      @id @default(autoincrement())
+  kisi_no        Int      @default(0)
+  aktif          String   @default("")
+  gorev          Int      @default(0)
+  ekleyen        String?
+  duzenleyen     String?
+  yorum          Int      @default(0)
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  eklenme_tarihi DateTime @default(dbgenerated())
+}
+
+model kisi_gorev_pozisyon {
+  numara     Int     @id @default(autoincrement())
+  ad         String
+  kisa_ad    String
+  aktif      String  @default("")
+  puan       Int     @default(0)
+  ekleyen    String?
+  duzenleyen String?
+  ozel_genel String?
+  uye_numara Int?
+}
+
+model kisi_gsm {
+  numara            Int       @id @default(autoincrement())
+  etiket            String?
+  telefonkod        String?
+  telefon           String?
+  kisi              Int?
+  aktif             String?
+  puan              Int?
+  varsayilan        String?
+  bilgi_mesaj       String?
+  ekleyen           String?
+  duzenleyen        String?
+  eklenme_tarihi    DateTime?
+  duzenlenme_tarihi DateTime?
+}
+
+model kisi_mail {
+  numara            Int       @id @default(autoincrement())
+  etiket            String?
+  mail              String?
+  kisi              Int?
+  aktif             String?
+  puan              Int?
+  varsayilan        String?
+  ekleyen           String?
+  duzenleyen        String?
+  eklenme_tarihi    DateTime?
+  duzenlenme_tarihi DateTime?
+  bilgi_mesaj       String?
+}
+
+model kisi_telefon {
+  numara            Int       @id @default(autoincrement())
+  etiket            String?
+  telefonkod        String?
+  telefon           String?
+  kisi              Int?
+  aktif             String?
+  puan              Int?
+  varsayilan        String?
+  pbxmi             String?
+  hatsayi           Int?
+  ekleyen           String?
+  duzenleyen        String?
+  eklenme_tarihi    DateTime?
+  duzenlenme_tarihi DateTime?
+}
+
+model komisyon_kesinti_sekli {
+  numara Int     @id @default(autoincrement())
+  ad     String?
+  aktif  String?
+  puan   Int?
+}
+
+model ksistemler {
+  numara     Int     @id @default(autoincrement())
+  aktif      String  @default("")
+  domain     Int     @default(0)
+  durumu     Int     @default(0)
+  servis     Int     @default(0)
+  ekleyen    String
+  duzenleyen String
+  saktif     String?
+  aciklama   String?
+  dbaglanti  Int     @default(0)
+  firma      Int     @default(0)
+
+  @@index([firma], name: "firma")
+}
+
+model ksistem_durumu {
+  numara Int    @id @default(autoincrement())
+  ad     String
+  aktif  String @default("")
+}
+
+model ksistem_logs {
+  numara             Int      @id @default(autoincrement())
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  islem_tarihi       DateTime @default(dbgenerated())
+  islem_tipi         String
+  islem_yapan        String
+  kontrol            String?
+  kontrol_eden       String?
+  k_tablo            String
+  k_numara           Int      @default(0)
+  k_ad               String
+  k_aktif            String   @default("")
+  k_domain           Int      @default(0)
+  k_domain_ad        String?
+  k_aciklama         String?
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  k_tarih            DateTime @default(dbgenerated())
+  k_gorusulen        String
+  k_ekleyen          String
+  k_duzenleyen       String
+  k_durumu           Int      @default(0)
+  k_durumu_ad        String?
+  k_sozlesme         Int      @default(0)
+  k_sozlesme_tarih   String?
+  k_yetkili          String
+  k_mt               Int      @default(0)
+  k_mt_ad            String?
+  k_sozlesme_durumu  String   @default("")
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  k_baslangic_tarihi DateTime @default(dbgenerated())
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  k_bitis_tarihi     DateTime @default(dbgenerated())
+  k_odeme_durumu     String   @default("")
+  k_yil              Int      @default(0)
+  k_firma            Int      @default(0)
+  k_firma_ad         String?
+  k_servis           Int      @default(0)
+  k_servis_ad        String?
+  k_dbaglanti        Int      @default(0)
+  k_dbaglanti_ad     String?
+  k_saktif           String?
+  k_icerik           String?
+  k_buyuklugu        Int      @default(0)
+}
+
+model ksistem_sozlesme {
+  numara           Int      @id @default(autoincrement())
+  ksistem          Int      @default(0)
+  aciklama         String?
+  sozlesme_durumu  String   @default("")
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  baslangic_tarihi DateTime @default(dbgenerated())
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  bitis_tarihi     DateTime @default(dbgenerated())
+  odeme_durumu     String   @default("0")
+  ekleyen          String
+  duzenleyen       String
+  durumu           Int      @default(0)
+  aktif            String   @default("")
+  yil              Int?
+  saktif           String?
+  buyuklugu        Int      @default(0)
+
+  @@index([ksistem], name: "ksistem")
+}
+
+model ksistem_sozlesme_durumu {
+  numara Int    @id @default(autoincrement())
+  ad     String
+  aktif  String @default("")
+}
+
+model kullanici_konfigurasyonlari {
+  numara                    Int     @id @default(autoincrement())
+  kullanici                 Int?
+  as_talimatlar_gosterilsin String?
+  as_reklamlar_gosterilsin  String?
+  ag_gorunum_tercihi        String?
+  sg_gorunum_tercihi        String?
+  og_gorunum_tercihi        String?
+  yg_gorunum_tercihi        String?
+}
+
+model logs {
+  numara             Int      @id @default(autoincrement())
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  islem_tarihi       DateTime @default(dbgenerated())
+  islem_tipi         String
+  islem_yapan        String
+  kontrol            String?
+  kontrol_eden       String?
+  k_tablo            String
+  k_numara           Int      @default(0)
+  k_ad               String
+  k_aktif            String   @default("")
+  k_domain           Int      @default(0)
+  k_domain_ad        String?
+  k_aciklama         String?
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  k_tarih            DateTime @default(dbgenerated())
+  k_gorusulen        String
+  k_ekleyen          String
+  k_duzenleyen       String
+  k_durumu           Int      @default(0)
+  k_durumu_ad        String?
+  k_sozlesme         Int      @default(0)
+  k_sozlesme_tarih   String?
+  k_yetkili          String
+  k_mt               Int      @default(0)
+  k_mt_ad            String?
+  k_sozlesme_durumu  String   @default("")
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  k_baslangic_tarihi DateTime @default(dbgenerated())
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  k_bitis_tarihi     DateTime @default(dbgenerated())
+  k_odeme_durumu     String   @default("")
+  k_yil              Int      @default(0)
+  k_firma            Int      @default(0)
+  k_firma_ad         String?
+  k_servis           Int      @default(0)
+  k_servis_ad        String?
+  k_dbaglanti        Int      @default(0)
+  k_dbaglanti_ad     String?
+  k_saktif           String?
+  k_icerik           String?
+}
+
+model mail_gonderim_ayarlari {
+  numara          Int    @id @default(autoincrement())
+  kategori        Int    @default(0)
+  hesap_numara    Int    @default(0)
+  alici_ad        String
+  aktif           String
+  alici_eposta    String
+  gonderim_durumu String
+  ekleyen         Int    @default(0)
+  duzenleyen      Int    @default(0)
+  ekleyen_ad      String
+  duzenleyen_ad   String
+}
+
+model mail_gonderim_genel_ayarlari {
+  numara          Int    @id @default(autoincrement())
+  kategori        Int    @default(0)
+  hesap_numara    Int    @default(0)
+  aktif           String
+  gonderen_ad     String
+  gonderen_eposta String
+  ekleyen         Int    @default(0)
+  duzenleyen      Int    @default(0)
+  ekleyen_ad      String
+  duzenleyen_ad   String
+}
+
+model mail_gonderim_kategorileri {
+  numara     Int     @id @default(autoincrement())
+  ad         String
+  aktif      String  @default("")
+  aciklama   String
+  puan       Int     @default(0)
+  ekleyen    String?
+  duzenleyen String?
+}
+
+model mail_logs {
+  numara         Int      @id @default(autoincrement())
+  /// This field's default value can currently not be parsed: `0000-00-00 00:00:00`.
+  tarih          DateTime @default(dbgenerated())
+  ekleyen        String
+  ekleyen_numara Int
+  icerik         String
+  tur            String?
+  firma          String?
+  kisi           String?
+  mail           String?
+}
+
+model moderator_gorusme_yorumu {
+  numara     Int     @id @default(autoincrement())
+  ad         String
+  aktif      String  @default("")
+  aciklama   String
+  puan       Int     @default(0)
+  ekleyen    String?
+  duzenleyen String?
+  ozel_genel String?
+  uye_numara Int?
+}
+
+model moderator_is_yorumu {
+  numara     Int     @id @default(autoincrement())
+  ad         String
+  aktif      String  @default("")
+  aciklama   String
+  puan       Int     @default(0)
+  ekleyen    String?
+  duzenleyen String?
+}
+
+model musteri_bilgisi {
+  numara     Int     @id @default(autoincrement())
+  ad         String
+  aktif      String  @default("")
+  aciklama   String
+  puan       Int     @default(0)
+  ekleyen    String?
+  duzenleyen String?
+}
+
+model m_banka {
+  numara          Int     @id @default(autoincrement())
+  aktif           String  @default("")
+  ad              String
+  kisa_ad         String
+  puan            Int     @default(0)
+  ekleyen         String?
+  duzenleyen      String?
+  ozel_genel      String?
+  uye_numara      Int?
+  tarih_sutunu    Int?
+  aciklama_sutunu Int?
+  tutar_sutunu    Int?
+  bakiye_sutunu   Int?
+}
+
+model m_banka_sube {
+  numara     Int     @id @default(autoincrement())
+  aktif      String  @default("")
+  ad         String
+  kisa_ad    String
+  puan       Int     @default(0)
+  ekleyen    String?
+  duzenleyen String?
+  ozel_genel String?
+  uye_numara Int?
+  il         Int?
+  ilce       Int?
+  adres      String?
+  telefon    String?
+  eposta     String?
+  banka      Int?
+  kod        String
+}
+
+model m_bordro {
+  numara      Int      @id @default(autoincrement())
+  cari_numara String
+  tarih       DateTime
+  durum       String   @default("0")
+  ekleyen     String?
+  duzenleyen  String?
+}
+
+model m_cek_senet {
+  numara           Int      @id @default(autoincrement())
+  aktif            String   @default("")
+  bordro_numara    Int
+  cari_numara      Int
+  tutar            Float
+  tur              Int
+  alttur           Int
+  eklenme_tarihi   DateTime
+  cek_banka        Int
+  cek_banka_sube   Int
+  vade_tarihi      DateTime
+  cek_senet_numara String
+  evrak_no         String
+  cek_cirolumu     String
+  cirolu_cek_isim  String
+  cek_odeme_yeri   String
+  durum            Int
+  evrak_resim      String
+  ekleyen          String?
+  duzenleyen       String?
+  soft_silinme     Int?     @default(0)
+  islem_tarihi     DateTime
+}
+
+model m_cek_senet_durumu {
+  numara   Int     @id @default(autoincrement())
+  ad       String
+  aktif    String  @default("")
+  ekleyen  String?
+  puan     Int?
+  sahiplik Int?
+}
+
+model m_cek_senet_durum_degisiklikleri {
+  numara                    Int      @id @default(autoincrement())
+  durum                     Int
+  cek_senet_numara          Int
+  tarih                     DateTime
+  tutar                     Float
+  aktarim_hedefi            String?
+  kasa_banka_id             Int?
+  cari_numara               Int
+  ekstra                    String?
+  hesap_hareketi_numara     Int?
+  kasa_hareketi_numara      Int?
+  banka_hareketi_numara     Int?
+  cek_senet_hareketi_numara Int?
+}
+
+model m_cek_senet_odeme_kayitlari {
+  numara           Int      @id @default(autoincrement())
+  cek_senet_numara Int
+  cari_numara      Int
+  tutar            Float
+  tarih            DateTime
+}
+
+model m_cek_senet_sahiplik {
+  numara  Int     @id @default(autoincrement())
+  ad      String
+  aktif   String  @default("")
+  ekleyen String?
+  puan    Int?
+}
+
+model m_cek_senet_tahsil_kayitlari {
+  numara           Int       @id @default(autoincrement())
+  cek_senet_numara Int
+  cari_numara      Int
+  tutar            Float
+  aktarim_hedefi   String?
+  kasa_banka_id    Int?
+  tarih            DateTime?
+}
+
+model m_cek_senet_turu {
+  numara  Int     @id @default(autoincrement())
+  ad      String
+  aktif   String  @default("")
+  ekleyen String?
+  puan    Int?
+}
+
+model m_hesap {
+  numara      Int     @id @default(autoincrement())
+  aktif       String  @default("")
+  ad          String
+  kisa_ad     String
+  kod         String
+  sube_kodu   String
+  sube_adi    String
+  hesap_adi   String
+  hesap_no    String
+  doviz_cinsi Int
+  iban        String
+  ilgili_kisi String
+  telefon     String
+  adres       String
+  puan        Int     @default(0)
+  banka       Int     @default(0)
+  sube        Int     @default(0)
+  ekleyen     String?
+  duzenleyen  String?
+  ozel_genel  String?
+  uye_numara  Int?
+}
+
+model m_pos {
+  numara     Int     @id @default(autoincrement())
+  aktif      String  @default("")
+  ad         String
+  banka      String
+  puan       Int     @default(0)
+  ekleyen    String?
+  duzenleyen String?
+  ozel_genel String?
+  uye_numara Int?
+  hesap      Int?
+}
+
+model m_pos_tanimlar {
+  numara                              Int     @id @default(autoincrement())
+  aktif                               String  @default("")
+  tahsilat_kodu                       String
+  tahsilat_sekli                      String
+  taksit_adeti                        String
+  taksit_araliklari                   String
+  banka_hesap_gecis_sekli             String
+  hesaba_gecis_suresi                 String
+  cari_hesap_kayit_sekli              String
+  komisyon_orani                      Float
+  komisyon_tutari                     Int     @default(0)
+  komisyon_kesinti_sekli              String
+  komisyon_gider_turu                 String
+  promosyon_orani                     Float
+  puan                                Int     @default(0)
+  ekleyen                             String?
+  duzenleyen                          String?
+  ozel_genel                          String?
+  uye_numara                          Int?
+  banka                               Int?
+  promosyon_tutari                    Float?
+  promosyon_kesinti_sekli             String?
+  promosyon_gider_kodu                String?
+  hizmet_orani                        Float?
+  hizmet_tutari                       String?
+  hizmet_kesinti_sekli                String?
+  genel_gider_kodu                    String?
+  hizmet_gider_kodu                   String?
+  aciklama                            String?
+  taksit_gunleri_haftasonuna_gelmesin String?
+  pos_numara                          Int?
+  kesinti_sekli                       Int?
+  kesinti_orani                       Float?
+  hesap                               Float?
+}
+
+model m_tip_tanimlari {
+  numara      Int     @id @default(autoincrement())
+  aktif       String  @default("")
+  tip         Int     @default(0)
+  ekleyen     String
+  duzenleyen  String
+  ad          String?
+  puan        Int     @default(0)
+  dekont_tipi Int     @default(0)
+}
+
+model neticesiz_is_durumu {
+  numara     Int     @id @default(autoincrement())
+  ad         String
+  aktif      String  @default("")
+  aciklama   String
+  puan       Int     @default(0)
+  ekleyen    String?
+  duzenleyen String?
+}
+
+model odemeler {
+  numara         Int       @id @default(autoincrement())
+  isturu         Int       @default(0)
+  gorusme_numara Int       @default(0)
+  firma_numara   Int       @default(0)
+  toplam         Float     @default(0)
+  alinan         Float     @default(0)
+  kalan          Float     @default(0)
+  odeme_turu     Int       @default(0)
+  mtemsilcisi    Int?
+  tarih          DateTime?
+  ekleyen        String?
+  duzenleyen     String?
+
+  @@index([gorusme_numara], name: "gorusme_numara")
+}
+
+model odeme_turu {
+  numara   Int    @id @default(autoincrement())
+  ad       String
+  aktif    String @default("")
+  aciklama String
+}
+
+model parametresi_iliskili_gorev_kategorileri {
+  numara        Int     @id @default(autoincrement())
+  kategori      Int?
+  altkategori   Int?
+  iliski_dizisi String?
+}
+
+model parametre_sabitleri {
+  numara Int     @id @default(autoincrement())
+  ad     String?
+}
+
+model para_birimleri {
+  numara             Int    @id @default(autoincrement())
+  ad                 String
+  kur                Float  @default(0)
+  kisa_ad            String
+  uluslararasi_birim String
+  aciklama_tr        String
+  aciklama_en        String
+}
+
+model prim_hedefleri {
+  numara     Int     @id @default(autoincrement())
+  personel   Int?
+  donemi     Int?
+  aciklama   String?
+  duzenleyen String?
+  ekleyen    String?
+  aktif      String?
+}
+
+model prim_hedefleri_detay {
+  numara          Int     @id @default(autoincrement())
+  hedef_numara    Int?
+  ps_tanim_numara Int?
+  alt_limit       Float?
+  ust_limit       Float?
+  aciklama        String?
+  ana_hedef       Float?
+}
+
+model prim_hedefleri_detay2 {
+  numara                      Int     @id @default(autoincrement())
+  hedef_numara                Int?
+  ps_gorev_bazli_tanim_numara Int?
+  basarildiysa                Float?
+  basarilamadiysa             Float?
+  aciklama                    String?
+}
+
+model prim_sistemi {
+  numara                                       Int       @id @default(autoincrement())
+  duzenlenme_tarihi                            DateTime?
+  ekleyen                                      String?
+  sirket_ust_carpan                            Float?
+  sirket_alt_carpan                            Float?
+  sifir                                        Float?
+  yenileme                                     Float?
+  zamaninda_biten_gorev_primi_carpani          Float?
+  gec_biten_gorev_primi_carpani                Float?
+  aciklama                                     String?
+  eklenme_tarihi                               DateTime?
+  tarih                                        DateTime?
+  duzenleyen                                   String?
+  aktif                                        String?   @default("1")
+  zamaninda_bitirilen_gorev_performans_carpani Float?
+  gecikmeli_bitirilen_gorev_performans_carpani Float?
+  hukmen_dusen_gorev_performans_carpani        Float?
+  cebren_dusurulen_gorev_performans_carpani    Float?
+}
+
+model prim_sistemi_detay {
+  ps_tanim_numara            Int
+  alt_limit_alti_icin_carpan Float
+  prim_sistemi_numara        Int
+  aciklama                   String?
+  eklenme_tarihi             DateTime?
+  numara                     Int       @id @default(autoincrement())
+  duzenleyen                 String?
+  duzenlenme_tarihi          DateTime?
+  ekleyen                    String?
+  alt_carpan                 Float?
+  ust_carpan                 Float?
+  ust_limit_ustu_icin_carpan Float?
+}
+
+model prim_sistemi_gorev_bazli_detay {
+  ps_gorev_bazli_tanim_numara Int
+  prim_sistemi_numara         Int
+  aciklama                    String?
+  eklenme_tarihi              DateTime?
+  numara                      Int       @id @default(autoincrement())
+  duzenleyen                  String?
+  duzenlenme_tarihi           DateTime?
+  ekleyen                     String?
+  basarildiysa                Float?
+  basarilamadiysa             Float?
+}
+
+model prim_sistemi_gorev_kurallari {
+  numara              Int       @id @default(autoincrement())
+  prim_sistemi_numara Int?
+  tutar               Float?
+  denetleme_tutar     Float?
+  kontrol_tutar       Float?
+  ekleyen             String?
+  eklenme_tarihi      DateTime?
+  duzenleyen          String?
+  duzenlenme_tarihi   DateTime?
+  gorev_numara        Int?
+  aktif               String?
+}
+
+model prim_sistemi_gorusme_yeri {
+  numara     Int     @id @default(autoincrement())
+  ad         String
+  kisa_ad    String
+  aktif      String  @default("")
+  puan       Int     @default(0)
+  ekleyen    String?
+  duzenleyen String?
+  ozel_genel String?
+  uye_numara Int?
+}
+
+model prim_sistemi_gorusme_yeri_detay {
+  ps_gorusme_yeri_numara Int
+  prim_sistemi_numara    Int
+  eklenme_tarihi         DateTime?
+  numara                 Int       @id @default(autoincrement())
+  duzenleyen             String?
+  duzenlenme_tarihi      DateTime?
+  ekleyen                String?
+  carpan                 Float?
+}
+
+model prim_sistemi_kurallar {
+  numara              Int       @id @default(autoincrement())
+  duzenlenme_tarihi   DateTime?
+  ekleyen             String?
+  prim_yuzdesi        Float?
+  is_turu             Int?
+  ornek_tahsilat      Float?
+  alt_limit           Float?
+  aciklama            String?
+  eklenme_tarihi      DateTime?
+  ust_limit           Float?
+  duzenleyen          String?
+  ps_tanim_numara     Int?
+  prim_sistemi_numara Int?
+  urun_hizmet_numara  Int?
+}
+
+model prim_sistemi_sifir_veya_yenileme_durumu {
+  numara     Int     @id @default(autoincrement())
+  ad         String
+  aktif      String  @default("")
+  aciklama   String
+  puan       Int     @default(0)
+  ekleyen    String?
+  duzenleyen String?
+  ozel_genel String?
+  uye_numara Int?
+}
+
+model prim_sistemi_tahsilat_yeri {
+  numara     Int     @id @default(autoincrement())
+  ad         String
+  kisa_ad    String
+  aktif      String  @default("")
+  puan       Int     @default(0)
+  ekleyen    String?
+  duzenleyen String?
+  ozel_genel String?
+  uye_numara Int?
+}
+
+model prim_sistemi_tahsilat_yeri_detay {
+  ps_tahsilat_yeri_numara Int
+  prim_sistemi_numara     Int
+  eklenme_tarihi          DateTime?
+  numara                  Int       @id @default(autoincrement())
+  duzenleyen              String?
+  duzenlenme_tarihi       DateTime?
+  ekleyen                 String?
+  carpan                  Float?
+}
+
+model prim_sistemi_tanimlar {
+  numara     Int     @id @default(autoincrement())
+  ad         String
+  kisa_ad    String
+  aktif      String  @default("")
+  puan       Int     @default(0)
+  ekleyen    String?
+  duzenleyen String?
+  ozel_genel String?
+  uye_numara Int?
+}
+
+model promosyon_kesinti_sekli {
+  numara Int     @id @default(autoincrement())
+  ad     String?
+  aktif  String?
+  puan   Int?
+}
+
+model promosyon_takibi {
+  numara         Int      @id @default(autoincrement())
+  ad             String
+  aktif          String   @default("")
+  firma          Int      @default(0)
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  tarih          DateTime @default(dbgenerated())
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  eklenme_tarihi DateTime @default(dbgenerated())
+  ekleyen        String
+  duzenleyen     String
+  aciklama       String?
+  gorusme_numara Int?
+}
+
+model rezervasyon_icerik {
+  numara                Int       @id @default(autoincrement())
+  kategori_numara       Int?
+  cari_hesap            Int?
+  baslangic_tarih       DateTime?
+  bitis_tarih           DateTime?
+  aciklama              String?
+  durum                 Int?
+  iskonto_orani         Float?
+  rezervasyon_tarihleri String?
+  firma_bilgi           String?
+  ust_hizmet            Int?
+  tip                   String?
+  islem_sureci          String?
+  mt_numara             Int?
+  iptal_tarihi          DateTime?
+  hesap_sabitleri       String?
+  operator_onayi        String?
+  yonetici_onayi        String?
+  blok_tarihler         String
+  gorev_bilgileri       String
+}
+
+model rezervasyon_indirim {
+  numara         Int    @id @default(autoincrement())
+  rezervasyon_no Int
+  gun            Int?
+  ucret          Float?
+}
+
+model rezervasyon_kategori {
+  numara        Int     @id @default(autoincrement())
+  ad            String?
+  ust_kategori  Int     @default(0)
+  aktif         Int     @default(1)
+  aciklama      String?
+  birim_fiyati  Float   @default(0)
+  islem_sayisi  Int     @default(1)
+  kullanim_turu Int     @default(1)
+  link          String
+  resim         String
+}
+
+model satis_fatura_kalemleri {
+  numara                Int     @id @default(autoincrement())
+  fatura_numara         Int     @default(0)
+  urun_hizmet_numara    Int?
+  urun_hizmet_turu      String?
+  miktar                Int?
+  birim                 Int?
+  birim_fiyat           Float?
+  birim_fiyat_doviz     Float?
+  indirim_orani         Int?
+  indirim_tutari        Float?
+  tutar                 Float?
+  kdv_orani             Int?
+  kdv_tutari            Float?
+  net_birim_fiyat       Float?
+  net_birim_fiyat_doviz Float?
+  satir_aciklamasi      String?
+  aciklama              String?
+  para_birimi           String?
+  uygulanan_kur         String?
+}
+
+model sektor {
+  numara            Int       @id @default(autoincrement())
+  ad                String
+  puan              Int?
+  aktif             String?
+  ekleyen           String?
+  duzenleyen        String?
+  eklenme_tarihi    DateTime?
+  duzenlenme_tarihi DateTime?
+  ozel_genel        String?
+  uye_numara        Int?
+}
+
+model seo_kelime {
+  numara                Int       @id @default(autoincrement())
+  aciklama              String?
+  ad                    String?
+  aktif                 String?
+  puan                  Int?
+  duzenleyen            String?
+  ekleyen               String?
+  eklenme_tarihi        DateTime?
+  resim_adres           String?
+  duzenlenme_tarihi     DateTime?
+  site_numara           Int?
+  aranacak_sayfa_sayisi Int?
+}
+
+model seo_kelimeler_calisma {
+  numara            Int       @id @default(autoincrement())
+  aktif             String?
+  diger             String?
+  duzenleyen        String?
+  ekleyen           String?
+  baslangic_tarihi  DateTime?
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  duzenlenme_tarihi DateTime? @default(dbgenerated())
+  eklenme_tarihi    DateTime?
+  site_numara       Int?
+  kelime_numara     Int?
+  seo_aktif         String?
+  bitis_tarihi      DateTime?
+  calismayi_yapan   String?
+}
+
+model seo_kelimeler_veri {
+  numara            Int       @id @default(autoincrement())
+  aktif             String?
+  sira              Int?
+  duzenleyen        String?
+  ekleyen           String?
+  tarih             DateTime?
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  duzenlenme_tarihi DateTime? @default(dbgenerated())
+  saat              DateTime?
+  site_numara       Int?
+  kelime_numara     Int?
+}
+
+model seo_site {
+  numara            Int       @id @default(autoincrement())
+  aciklama          String?
+  ad                String?
+  aktif             String?
+  puan              Int?
+  duzenleyen        String?
+  ekleyen           String?
+  eklenme_tarihi    DateTime?
+  resim_adres       String?
+  duzenlenme_tarihi DateTime?
+}
+
+model serbest_sms_mesajlari {
+  numara      Int     @id @default(autoincrement())
+  icerik      String
+  aktif       String  @default("")
+  kategori_no Int     @default(0)
+  ekleyen     String?
+  puan        Int?
+  duzenleyen  String?
+  ad          String?
+}
+
+model serbest_sms_mesaj_kategori {
+  numara     Int     @id @default(autoincrement())
+  ad         String
+  aktif      String  @default("")
+  ekleyen    String?
+  puan       Int?
+  duzenleyen String?
+  isturu     Int?
+  ozel_genel String?
+  uye_numara Int?
+}
+
+model sistem_erisim_bilgileri {
+  numara                        Int       @id @default(autoincrement())
+  kullanici_adi                 String
+  sifre                         String
+  link                          String
+  hesap_turu                    Int       @default(0)
+  ekleyen                       Int?      @default(0)
+  duzenleyen                    Int?      @default(0)
+  gizli_soru                    String?
+  gizli_cevap                   String?
+  ana_hesap                     String?
+  eklenme_tarihi                DateTime?
+  duzenlenme_tarihi             DateTime?
+  son_duzenlemeden_onceki_bilgi String?
+  diger                         String?
+  firma                         Int?
+
+  @@index([firma], name: "firma")
+  @@index([hesap_turu], name: "hesap_turu")
+}
+
+model sistem_erisim_bilgileri_hatirlatma_mailleri {
+  numara               Int       @id @default(autoincrement())
+  aciklama             String?
+  /// This field's default value can currently not be parsed: `0000-00-00 00:00:00`.
+  tarih                DateTime  @default(dbgenerated())
+  ekleyen              String
+  duzenleyen           String
+  durumu               Int       @default(0)
+  bilgilendirme_icerik String?
+  hesap_turu           Int?
+  kisi_numara          Int?
+  firma                Int?
+  hesap_turu_numara    Int?
+  mail                 String?
+  etiket               String?
+  bilgilendirme_turu   Int?
+  gonderim_durumu      String?
+  gonderilecek_tarih   DateTime?
+  gonderen             String?
+  gonderim_tarihi      DateTime?
+  onay_durumu          String?
+  serbest              String?
+  mtemsilcisi          Int?
+
+  @@index([firma], name: "firma")
+  @@index([hesap_turu], name: "hesap_turu")
+  @@index([onay_durumu], name: "onay_durumu")
+}
+
+model sistem_erisim_bilgileri_hatirlatma_smsleri {
+  numara               Int       @id @default(autoincrement())
+  aciklama             String?
+  /// This field's default value can currently not be parsed: `0000-00-00 00:00:00`.
+  tarih                DateTime  @default(dbgenerated())
+  ekleyen              String
+  duzenleyen           String
+  durumu               Int       @default(0)
+  bilgilendirme_icerik String?
+  hesap_turu           Int?
+  kisi_numara          Int?
+  firma                Int?
+  hesap_turu_numara    Int?
+  telefon              String?
+  etiket               String?
+  bilgilendirme_turu   Int?
+  gonderim_durumu      String?
+  gonderilecek_tarih   DateTime?
+  gonderen             String?
+  gonderim_tarihi      DateTime?
+  onay_durumu          String?
+  serbest              String?
+  mtemsilcisi          Int?
+
+  @@index([firma], name: "firma")
+  @@index([hesap_turu], name: "hesap_turu")
+  @@index([onay_durumu], name: "onay_durumu")
+}
+
+model sistem_erisim_bilgileri_hesap_turu {
+  numara               Int     @id @default(autoincrement())
+  ad                   String
+  aktif                String  @default("")
+  aciklama             String
+  puan                 Int     @default(0)
+  ekleyen              String?
+  duzenleyen           String?
+  bilgilendirme_maili1 String?
+  bilgilendirme_maili2 String?
+  bilgilendirme_maili3 String?
+  bilgilendirme_maili4 String?
+  bilgilendirme_maili5 String?
+  bilgilendirme_maili6 String?
+  ekleyen_numara       Int?
+  duzenleyen_numara    Int?
+  uye_numara           Int?
+  ozel_genel           String?
+}
+
+model smsler {
+  numara               Int       @id @default(autoincrement())
+  aciklama             String?
+  /// This field's default value can currently not be parsed: `0000-00-00 00:00:00`.
+  tarih                DateTime  @default(dbgenerated())
+  ekleyen              String
+  duzenleyen           String
+  durumu               Int       @default(0)
+  bilgilendirme_icerik String?
+  tur                  Int?
+  kisi_numara          Int?
+  firma                Int?
+  tur_numara           Int?
+  telefon              String?
+  etiket               String?
+  bilgilendirme_turu   Int?
+  gonderim_durumu      String?
+  gonderilecek_tarih   DateTime?
+  gonderen             String?
+  gonderim_tarihi      DateTime?
+  onay_durumu          String?
+  serbest              String?
+  mtemsilcisi          Int?
+
+  @@index([onay_durumu], name: "onay_durumu")
+}
+
+model sms_logs {
+  numara         Int      @id @default(autoincrement())
+  /// This field's default value can currently not be parsed: `0000-00-00 00:00:00`.
+  tarih          DateTime @default(dbgenerated())
+  ekleyen        String
+  ekleyen_numara Int
+  icerik         String
+  tur            String?
+  firma          String?
+  kisi           String?
+  telefon        String?
+}
+
+model sms_mesajlari {
+  numara      Int     @id @default(autoincrement())
+  icerik      String
+  aktif       String  @default("")
+  kategori_no Int     @default(0)
+  ekleyen     String?
+  puan        Int?
+  duzenleyen  String?
+  ad          String?
+}
+
+model sms_mesaj_kategori {
+  numara     Int     @id @default(autoincrement())
+  ad         String
+  aktif      String  @default("")
+  ekleyen    String?
+  puan       Int?
+  duzenleyen String?
+  isturu     Int?
+  ozel_genel String?
+  uye_numara Int?
+}
+
+model sozlesme_ekler {
+  numara         Int     @id @default(autoincrement())
+  resim_adres    String
+  aktif          String  @default("")
+  puan           Int?
+  ekleyen        String?
+  duzenleyen     String?
+  sozlesme       Int?
+  eklenme_tarihi String?
+}
+
+model stok_birimleri {
+  numara             Int    @id @default(autoincrement())
+  ad                 String
+  uluslararasi_birim String
+  aciklama_tr        String
+  aciklama_en        String
+}
+
+model stok_hareketleri {
+  numara                    Int       @id @default(autoincrement())
+  islem_tarihi              DateTime?
+  aciklama                  String?
+  firma                     Int?
+  ekleyen                   String?
+  tutar_doviz               Float?
+  kdv_dahil_tutar_doviz     Float?
+  depo                      Int?
+  eklenme_tarihi            DateTime?
+  duzenleyen                String?
+  duzenlenme_tarihi         DateTime?
+  tutar                     Float?
+  kdv_dahil_tutar           Float?
+  stok_numara               Int?
+  ekleyen_numara            Int?
+  duzenleyen_numara         Int?
+  hesap_hareketi_numara     Int?
+  kalem_numara              Int?
+  miktar                    Int?
+  kasa_hareketi_numara      Int?
+  banka_hareketi_numara     Int?
+  cek_senet_hareketi_numara Int?
+  evrak_no                  String?
+  sube                      Int?
+  dekont_tipi               Int?
+}
+
+model stok_hizmet_kartlari {
+  numara            Int       @id @default(autoincrement())
+  ad                String
+  kod               String?
+  stok_hizmet_turu  String    @default("")
+  birim             Int?
+  satis_kdv_orani   Float?
+  doviz_cinsi       Int?
+  alis_fiyati       Float?
+  alis_p_birimi     Int?
+  satis_fiyati      Float?
+  satis_p_birimi    Int?
+  tur               Int?
+  kategori          Int?
+  birim2            Int?
+  birim3            Int?
+  miktar            Int?
+  minimum_miktar    Int?
+  maksimum_miktar   Int?
+  temin_suresi      Int?
+  resim             String?
+  ekleyen           String?
+  duzenleyen        String?
+  ekleyen_numara    Int?
+  eklenme_tarihi    DateTime?
+  duzenleyen_numara Int?
+  duzenlenme_tarihi DateTime?
+  alis_kdv_orani    Float?
+  depo              Int?
+  aktif             String?   @default("1")
+}
+
+model stok_turu {
+  numara Int    @id @default(autoincrement())
+  ad     String
+  puan   Int?
+}
+
+model sube {
+  numara     Int     @id @default(autoincrement())
+  ad         String
+  kisa_ad    String
+  aktif      String  @default("")
+  puan       Int     @default(0)
+  ekleyen    String?
+  duzenleyen String?
+  ozel_genel String?
+  uye_numara Int?
+}
+
+model tahsilat_sekli {
+  numara Int     @id @default(autoincrement())
+  ad     String?
+  aktif  String?
+  puan   Int?
+}
+
+model talep_durumu {
+  numara     Int     @id @default(autoincrement())
+  ad         String
+  aktif      String  @default("")
+  aciklama   String
+  puan       Int     @default(0)
+  ekleyen    String?
+  duzenleyen String?
+}
+
+model tarihce_olay {
+  numara         Int       @id @default(autoincrement())
+  ad             String
+  aktif          String    @default("")
+  icerik         String?
+  puan           Int?
+  mtemsilcisi    Int?
+  ekleyen        String?
+  duzenleyen     String?
+  eklenme_tarihi DateTime?
+  personel       Int?
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  tarih          DateTime? @default(dbgenerated())
+}
+
+model tum_sozlesmeler {
+  numara                Int       @id @default(autoincrement())
+  firma                 Int       @default(0)
+  aciklama              String?
+  sozlesme_durumu       String    @default("")
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  baslangic_tarihi      DateTime  @default(dbgenerated())
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  bitis_tarihi          DateTime  @default(dbgenerated())
+  uyelik_buyuklugu      Int?
+  odeme_durumu          String    @default("0")
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  guncelleme            DateTime  @default(dbgenerated())
+  odeme_miktari         Float?
+  isturu                Int?
+  eklenme_tarihi        DateTime?
+  kontrol               String?   @default("0")
+  kontrol_eden          String?
+  sozlesme_devam_durumu Int?
+  ad                    String?
+  hediye                String?
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  doldurulma_tarihi     DateTime? @default(dbgenerated())
+  bitisyok              String?
+  link1                 String?
+  link2                 String?
+  link3                 String?
+  link4                 String?
+  link5                 String?
+  ekleyen               String?
+  duzenleyen            String?
+  duzenlenme_tarihi     DateTime?
+  baglantili_konum      String?
+  baglantili_numara     Int?
+  durduruldu            Int?
+  kalan_bakiye          Float?
+  durdurulma_tarihi     DateTime?
+  rezervasyon_durumu    String?
+  rezervasyon_numara    Int?
+  gorusme_numara        Int?
+  hizmet_turu           Int
+
+  @@index([firma], name: "firma")
+  @@index([kontrol], name: "kontrol")
+}
+
+// The underlying table does not contain a valid unique identifier and can therefore currently not be handled.
+model t_login_attempts {
+  id           Int       @id @default(autoincrement())
+  kullanici    String
+  tarih_zaman  String
+  firma_kodu   String
+  aciklama     String?
+  tarih_zaman2 DateTime?
+  ip_adresi    String?
+}
+
+model web_genel {
+  numara     Int     @id @default(autoincrement())
+  domain     String
+  coklu_veri String?
+}
+
+model web_modulleri {
+  numara            Int       @id @default(autoincrement())
+  ad                String
+  puan              Int       @default(0)
+  aktif             String    @default("")
+  icerik            String
+  kod               String?
+  eklenme_tarihi    DateTime?
+  duzenlenme_tarihi DateTime?
+  ust_kategori      Int?
+  veri_tipi         String?
+  veri_yazi         String?
+  veri_link         String?
+  veri_resim        String?
+  dil               Int?
+  klasor_adi        String?
+  coklu_veri        String?
+  ust_modul         Int
+  b64_resim         String
+  domain            String
+  sayfa_adi         String
+}
+
+model web_temalar_ve_sabitleri {
+  numara            Int       @id @default(autoincrement())
+  ad                String
+  puan              Int       @default(0)
+  aktif             String    @default("")
+  icerik            String
+  kod               String?
+  eklenme_tarihi    DateTime?
+  duzenlenme_tarihi DateTime?
+  ust_kategori      Int?
+  veri_tipi         String?
+  veri_yazi         String?
+  veri_link         String?
+  veri_resim        String?
+  dil               Int?
+  klasor_adi        String?
+  coklu_veri        String?
+  b64_resim         String
+  domain            String
+  ust_modul         Int
+  bagli_modul       Int
+  data_type         String
+}
+
+model web_veri_tipleri {
+  numara   Int    @id @default(autoincrement())
+  ad       String
+  aciklama String
+}
+
+model yasam_agaci_dallari {
+  numara            Int       @id @default(autoincrement())
+  ad                String
+  puan              Int       @default(0)
+  tip               String?
+  aktif             String    @default("")
+  icerik            String
+  icerik_goster     String    @default("")
+  title             String?
+  description       String?
+  keywords          String?
+  htaccess_etiket   String?
+  kod               String?
+  eklenme_tarihi    DateTime?
+  grup              Int       @default(0)
+  kategori          Int?
+  altkategori       Int       @default(0)
+  altkategori2      Int       @default(0)
+  altkategori3      Int       @default(0)
+  birim             Int?
+  kdv_orani         Int?
+  para_birimi       Int?
+  birim_fiyat       Float?
+  birim_fiyat_doviz Float?
+  muh_kodu1         String?
+  muh_kodu2         String?
+  kdv_m_kodu        String?
+  ozel_genel        String
+}
+
+model yduyurular {
+  numara         Int       @id @default(autoincrement())
+  ad             String
+  aktif          String    @default("")
+  icerik         String?
+  puan           Int?
+  mtemsilcisi    Int?
+  ekleyen        String?
+  duzenleyen     String?
+  eklenme_tarihi DateTime?
+  personel       Int?
+  ozet           String?
+}
+
+model yetki_kategorileri {
+  numara       Int     @id @default(autoincrement())
+  ad           String?
+  ust_kategori Int     @default(0)
+  aktif        Int     @default(1)
+  aciklama     String?
+  yetkiler     String
+  korumali     String
+  varsayilan   String
+}
+
+model yetki_talepleri {
+  numara                   Int      @id @default(autoincrement())
+  aciklama                 String?
+  yetkiler                 String
+  temel_yetkiler           String
+  diger_yetkiler           String
+  yetki_kategorileri       String
+  temel_yetki_kategorileri String
+  diger_yetki_kategorileri String
+  talep_tarihi             DateTime
+  islem_tarihi             DateTime
+  kullanici                Int
+  islem_yapan              String
+  islem_yapan_numara       Int
+  talep_nedeni             String
+}
+
+model ygiris {
+  numara      Int     @id @default(autoincrement())
+  ad          String
+  resim_adres String
+  aktif       String  @default("")
+  genislik    Int     @default(0)
+  yukseklik   Int     @default(0)
+  link        String?
+  altbilgi    String?
+  blank       String?
+  puan        Int?
+  mtemsilcisi Int?
+  ekleyen     String?
+  duzenleyen  String?
+  personel    Int?
+}
+
+model ygsozler {
+  numara      Int     @id @default(autoincrement())
+  ad          String
+  aktif       String  @default("")
+  altbilgi    String?
+  puan        Int?
+  mtemsilcisi Int?
+  ekleyen     String?
+  duzenleyen  String?
+  personel    Int?
+}
+
+model yhatirlatmalar_mailing {
+  numara          Int       @id @default(autoincrement())
+  tarih           DateTime?
+  gonderen        String?
+  gonderen_numara Int?
+}
+
+model yonetim_gorev_sistemi_kisisel_yetkileri {
+  numara         Int       @id @default(autoincrement())
+  yonetici       Int       @default(0)
+  ekleyen        String?
+  gorev_turu     Int?      @default(0)
+  yetki_turu     String    @default("0")
+  yetki_derecesi Int       @default(0)
+  duzenleyen     String
+  eklenme_tarihi DateTime?
+
+  @@index([gorev_turu], name: "hesap_turu")
+  @@index([yetki_derecesi], name: "yetki_derecesi")
+  @@index([yetki_turu], name: "yetki_turu")
+  @@index([yonetici], name: "yonetici_numara")
+}
+
+model yonetim_grup {
+  numara     Int     @id @default(autoincrement())
+  ad         String
+  puan       Int     @default(0)
+  aktif      String  @default("")
+  ekleyen    String?
+  duzenleyen String?
+}
+
+model yonetim_kullanici_yetkileri {
+  numara    Int     @id @default(autoincrement())
+  yetkiler  String?
+  kullanici Int     @default(0)
+  puan      Int     @default(0)
+}
+
+model yonetim_sistem_erisim_bilgileri_yetkileri {
+  numara         Int       @id @default(autoincrement())
+  yonetici       Int       @default(0)
+  ekleyen        String?
+  hesap_turu     Int?      @default(0)
+  yetki_turu     String    @default("0")
+  eklenme_tarihi DateTime?
+
+  @@index([hesap_turu], name: "hesap_turu")
+  @@index([yetki_turu], name: "yetki_turu")
+  @@index([yonetici], name: "yonetici_numara")
+}
+
+model yonetim_yetki {
+  numara         Int     @id @default(autoincrement())
+  ad             String
+  puan           Int     @default(0)
+  aktif          String  @default("")
+  ekleyen        String?
+  duzenleyen     String?
+  aciklama       String?
+  kategori       Int
+  yetki_kategori Int
+}
+
+model yonetim_yetki_grup {
+  numara     Int @id @default(autoincrement())
+  yetki_no   Int @default(0)
+  yetki_grup Int @default(0)
+  puan       Int @default(0)
+}
+
+model yonetim_yetki_kategori {
+  numara     Int     @id @default(autoincrement())
+  ad         String
+  aktif      String  @default("")
+  ekleyen    String?
+  puan       Int?
+  duzenleyen String?
+}
+
+model yonetim_yetki_kullanici {
+  numara    Int @id @default(autoincrement())
+  yetki_no  Int @default(0)
+  kullanici Int @default(0)
+  puan      Int @default(0)
+}
+
+model yonlendirme_is_durumu {
+  numara     Int     @id @default(autoincrement())
+  ad         String
+  aktif      String  @default("")
+  aciklama   String
+  puan       Int     @default(0)
+  ekleyen    String?
+  duzenleyen String?
+}
+
+model yonlendirme_takip {
+  numara             Int       @id @default(autoincrement())
+  gorusme_numara     Int?
+  mtemsilcisi        Int?
+  yonlendiren        Int?
+  gorusme_tarihi     DateTime?
+  firma              Int?
+  yonlendirme_numara Int?
+}
+
+model yorum {
+  numara     Int     @id @default(autoincrement())
+  ad         String
+  kisa_ad    String
+  aktif      String  @default("")
+  puan       Int     @default(0)
+  ekleyen    String?
+  duzenleyen String?
+  ozel_genel String?
+  uye_numara Int?
+}
+
+model ytalimat {
+  numara          Int      @id @default(autoincrement())
+  ad              String
+  aciklama        String
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  verilme_tarihi  DateTime @default(dbgenerated())
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  yapilacak_tarih DateTime @default(dbgenerated())
+  aktif           String   @default("")
+  puan            Int      @default(0)
+  mtemsilcisi     Int      @default(0)
+  ekleyen         String?
+  duzenleyen      String?
+  is_durumu       Int?
+  bolge           Int?
+  goruldu         String?
+}
+
+model ytelefonla_yonlendirme {
+  numara                       Int       @id @default(autoincrement())
+  firma                        Int       @default(0)
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  tarih                        DateTime  @default(dbgenerated())
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  tarih2                       DateTime  @default(dbgenerated())
+  saat                         String
+  civarinda                    String?
+  aciklama                     String
+  mtemsilcisi                  Int       @default(0)
+  aktif                        String    @default("")
+  puan                         Int       @default(0)
+  yonlendiren                  Int       @default(0)
+  is_durumu                    Int       @default(0)
+  ekleyen                      String
+  duzenleyen                   String
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  eklenme_tarihi               DateTime  @default(dbgenerated())
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  guncellenme_tarihi           DateTime  @default(dbgenerated())
+  telefonda_gorusulen          Int       @default(0)
+  gorusulecek_kisi             Int       @default(0)
+  kalite                       Int?
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  telefon_tarihi               DateTime  @default(dbgenerated())
+  /// This field's default value can currently not be parsed: `0000-00-00`.
+  yonlendirme_tarihi           DateTime  @default(dbgenerated())
+  is_ucreti                    Float?
+  islembitti                   String    @default("")
+  goruldu                      String    @default("")
+  bitis_tarihi                 DateTime?
+  mt_sabitle                   String?
+  yn_sabitle                   String?
+  mtsil                        Int?      @default(0)
+  onay_tarihi                  DateTime?
+  onaylanmama_sebebi           String?
+  tfiyat                       Float?
+  admin_gordu                  String?
+  yonlendirenin_uyarma_tarihi  DateTime?
+  mtemsilcisinin_uyarma_tarihi DateTime?
+
+  @@index([islembitti], name: "islembitti")
+  @@index([mtemsilcisi], name: "mtemsilcisi")
+}
+
+model ytelefonla_yonlendirme_mailing {
+  numara          Int       @id @default(autoincrement())
+  tarih           DateTime?
+  gonderen        String?
+  gonderen_numara Int?
+}
+
+model zamanlanmis_gorevler {
+  numara                     Int       @id @default(autoincrement())
+  gorev_kategori             Int
+  gorev_altkategori          Int
+  zamanlama_turu             String?
+  tekrar_sayisi              Int?
+  gorevlendirilecek_personel String?
+  gorevlendirme_bitis_tarihi DateTime?
+  termin_dk                  Int?
+  termin_saat                Int?
+  termin_gun                 Int?
+  ekleyen                    String?
+  duzenleyen                 String?
+  eklenme_tarihi             DateTime?
+  duzenlenme_tarihi          DateTime?
+  aciklama                   String?
+  onem_derecesi              Int?
+  gorev_basligi              String?
+  firma                      Int?
+  aktif                      String?
+  saat                       String?
+  gun_ay                     String?
+  gun_hafta                  String?
+  ay                         String?
+  muhlet_bitis_tarihi        DateTime?
+  muhlet_bitis_saati         DateTime?
+  karmasa_coz                String?
+  opsiyonel                  String?
+  prim_hesaplanacak          String?
+  prim_hesaplanacak_ilk      String?
+  prim                       Float?
+  bekleme_notu               String?
+  beklemede                  String?
+  bekleme_baslangic_tarihi   DateTime?
+  bekleme_bitis_tarihi       DateTime?
+  vekalet                    String?
+  vekalet_notu               String?
+  vekalet_baslangic_tarihi   DateTime?
+  vekalet_bitis_tarihi       DateTime?
+  vekalet_eden_personel      String?
+  simulasyonda_kullan        String?
+  gorev_parametreleri        String?
+  iliskili_parametreler      String?
+  onemli                     String?
+  zorunlu_prim               String?
+}
+
+
+
+model yonetim {
+  numara                              Int       @id @default(autoincrement())
+  kullanici                           String    @default("")
+  sifre                               String    @default("")
+  grup_no                             Int       @default(0)
+  puan                                Int       @default(0)
+  aktif                               String    @default("")
+  ekleyen                             String?
+  ip_kontrol                          String    @default("")
+  ip                                  String?
+  duzenleyen                          String?
+  ad                                  String?
+  kisa_ad                             String
+  renk                                String?
+  dogum_tarihi                        DateTime?
+  listede_goruntulenmesin             String?
+  arac_yetki                          String?
+  baglanti_tipi                       Int?
+  ip1                                 String?
+  ip2                                 String?
+  ip3                                 String?
+  ip4                                 String?
+  ip5                                 String?
+  ip6                                 String?
+  ip7                                 String?
+  ip8                                 String?
+  ip9                                 String?
+  ip10                                String?
+  eposta                              String?
+  gsm                                 String?
+  eposta_istiyor                      String?
+  sms_istiyor                         String?
+  gsmkod                              String?
+  musteri_baglantisinda_goruntulensin String?
+  musteri_temsilcisi                  Int?
+  cinsiyet                            String?
+  satis_istatistiklerinde_goster      String?
+  dahili                              String?
+  tahsilat_primine_dahil              String?
+  gorev_primine_dahil                 String?
+  yetkiler                            String
+}
+
+
+
+model User {
+  id            Int       @default(autoincrement()) @id
+  name          String?
+  email         String?   @unique
+  emailVerified DateTime? @map(name: "email_verified")
+  image         String?
+  createdAt     DateTime  @default(now()) @map(name: "created_at")
+  updatedAt     DateTime  @updatedAt @map(name: "updated_at")
+
+
+  @@map(name: "users")
+}
+
+model Account {
+  id                 Int       @default(autoincrement()) @id
+  compoundId         String    @unique @map(name: "compound_id")
+  userId             Int       @map(name: "user_id")
+  providerType       String    @map(name: "provider_type")
+  providerId         String    @map(name: "provider_id")
+  providerAccountId  String    @map(name: "provider_account_id")
+  refreshToken       String?   @map(name: "refresh_token")
+  accessToken        String?   @map(name: "access_token")
+  accessTokenExpires DateTime? @map(name: "access_token_expires")
+  createdAt          DateTime  @default(now()) @map(name: "created_at")
+  updatedAt          DateTime  @default(now()) @map(name: "updated_at")
+
+  @@index([providerAccountId], name: "providerAccountId")
+  @@index([providerId], name: "providerId")
+  @@index([userId], name: "userId")
+
+  @@map(name: "accounts")
+}
+
+model Session {
+  id           Int      @default(autoincrement()) @id
+  userId       Int      @map(name: "user_id")
+  expires      DateTime
+  sessionToken String   @unique @map(name: "session_token")
+  accessToken  String   @unique @map(name: "access_token")
+  createdAt    DateTime @default(now()) @map(name: "created_at")
+  updatedAt    DateTime @default(now()) @map(name: "updated_at")
+
+  @@map(name: "sessions")
+}
+
+model VerificationRequest {
+  id         Int      @default(autoincrement()) @id
+  identifier String
+  token      String   @unique
+  expires    DateTime
+  createdAt  DateTime @default(now()) @map(name: "created_at")
+  updatedAt  DateTime @default(now()) @map(name: "updated_at")
+
+  @@map(name: "verification_requests")
+}
+
```


